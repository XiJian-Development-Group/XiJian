# 独立开发者工具包（DevKit）的运行时依赖。
#
# 这些是冻结二进制包唯一需要的第三方包。
# DevKit 使用的其余部分（ApiError 基类、提交 ID 生成器、时间辅助函数）
# 均已内置在 devkit/_vendor.py 中，无任何外部依赖，因此二进制包
# 永远不会引入 xijian_api 或 Flask。
#
# 安装方式:  pip install -r requirements.txt
# （build-devkit.sh 会在 devkit/.venv 内为你执行）

pywebview>=5.0      # 原生 webview 窗口（WKWebView / WebView2 / webkitgtk）
py7zr>=0.21         # 7Z 固态压缩包（若缺失则回退到 zip —— 参见 __init__.py）
