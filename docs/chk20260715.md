# 文档与代码一致性检查报告

> 检查日期：2026-07-15  
> 检查范围：`docs/`（排除 `已弃置/`）、`core/`、`devkit/`（排除 `macapp/`）  
> 参考基准：`docs/Dev. Function List功能清单v2.md` (v2.5, 2026-07-11)  
> 备注：`devkit/Problems.md` 已记录部分差距，本文档在此基础上结合全量文档与代码进行补全、归类与细化

---

## 目录

1. [核心架构层面的根本性差距](#1-核心架构层面的根本性差距)
2. [文档之间的内容不一致](#2-文档之间的内容不一致)
3. [A. 用户功能模块：功能清单与代码实现的差距](#3-a-用户功能模块功能清单与代码实现的差距)
   - [A1. 记忆系统](#a1-记忆系统)
   - [A2. OpenAI 兼容的 AI 模块](#a2-openai-兼容的-ai-模块)
   - [A3. 角色与状态系统](#a3-角色与状态系统)
   - [A4. 模拟世界系统](#a4-模拟世界系统)
   - [A5. 安全模块](#a5-安全模块)
   - [A6. 实时通话](#a6-实时通话)
   - [A7. 主动发起聊天或通话](#a7-主动发起聊天或通话)
   - [A8. 桌宠 / 动态壁纸](#a8-桌宠--动态壁纸)
4. [B. Apple TouchBar & Dynamic Island](#4-b-apple-touchbar--dynamic-island)
5. [C. Development Kit (开发者工具)](#5-c-development-kit开发者工具)
6. [API 文档与实际路由的不一致](#6-api-文档与实际路由的不一致)
7. [配置与部署文档的不一致](#7-配置与部署文档的不一致)
8. [测试与质量保障缺口](#8-测试与质量保障缺口)

---

## 1. 核心架构层面的根本性差距

| 编号 | 问题 | 详述 | 证据位置 |
|------|------|------|----------|
| ARCH-01 | **完全没有 SQLite 持久化** | 功能清单为几乎每个模块定义了 SQLite 表（附录 A 约 40 张）。实际所有数据存于 `core/xijian_api/stubs/state.py` 的进程内 dict（内存态），全仓库无 `sqlite3`/`CREATE TABLE`/`sqlalchemy`，进程重启即丢失，并发安全未处理。 ;;==**严重缺失，等待补全实现，最高优先级**== | `devkit/Problems.md:18`、`stubs/state.py` |
| ARCH-02 | **无 MCP 服务端** | 功能清单多处依赖 MCP（A2 工具调用、A5.2 桌面控制防护、A8 桌宠控制），但源码无 MCP 服务端实现（所有 `mcp` 命中均在 `core/.venv/` 第三方包内）。 ;;==**及时实现**== | `devkit/Problems.md:19` |
| ARCH-03 | **AI 后端引擎与 v2.1 选型不符** | v2.1 锁定 MeloTTS / DiffSinger / bge-m3 / Qwen2.5-7B。实际 `ai/backends/{mlx,gguf}` 是对 `mlx_lm`/`llama_cpp`/`mlx_audio`/`stable_diffusion_cpp` 等**可选外部库**的封装，库与权重均不随仓库发布；未安装时 `is_available()=False` 返回 503，唯一始终可用的是 `mock` 后端（脚本化测试桩）。视频后端探测的 `mlx_video`/`stable_diffusion_cpp_video` 库不存在，永远 503。 ;;==**需要修改文档要求与代码，等待修改、实现和确认**== | `devkit/Problems.md:20`、`core/xijian_api/ai/backends/` |
| ARCH-04 | **指定嵌入模型 bge-m3 未接入** | `memory_entries.embedding` 列在 `stubs/memory.py` 的 `_new_entry()` 中被注释掉；`recall_search()` 用子字符串匹配而非向量检索，与 A1.2 的 embedding 检索、A2 的 bge-m3 选型不符。 ;;==**等待补全实现**== | `devkit/Problems.md:21`、`stubs/memory.py:175` |
| ARCH-05 | **核心服务层缺失** | `Dev.md §3` 规划 `core/services/{character,interaction,world,memory,protection,notifier}`、`core/ai/`、`core/store/`、`core/models/` 等目录，实际仓库无这些目录，业务逻辑分散在 `stubs/` 与 `routes/` 中，未形成分层架构。 ;;==**等待补全实现**== | `Dev.md:131-158`、实际目录结构 |

---

## 2. 文档之间的内容不一致

| 编号 | 文档 A | 文档 B | 不一致点 | 影响 |
|------|--------|--------|----------|------|
| DOC-01 | `README.md:30` | `CoreStartupGuide.md:17` | README 称「默认监听 `127.0.0.1`」，Startup Guide 称「默认监听 `0.0.0.0`（全网卡，**允许外部访问**）」;; ==**声明：未在README.md中找都有关内容，该项待确认，但无论如何，正确监听应当为127.0.0.1，需要在代码中进一步确认**== | 安全基线冲突：生产若按 README 配置会拒绝外部访问；按 Guide 配置会暴露本地 API |
| DOC-02 | `README.md:32-36` | `CoreStartupGuide.md:24-30` | 设备最低配置不一致：README 要求 Mac 16GB 内存/6GB 可用/24GB 磁盘，Guide 要求 16GB+ 统一内存；Windows README 要求 20GB 显存/26GB 磁盘，Guide 要求 16GB+ 显存/内存;; ==**遵循README.md所述内容，CoreStartupGuide.md已经部分修改，等待确认（需要补充内容）**== | 部署文档互相矛盾，运维无法按单一标准核对 |
| DOC-03 | `Dev.md:19-22` | `CoreStartupGuide.md:24-30` | 目标平台版本不一致：Dev.md 称 macOS ≥ 26.0 / Windows ≥ 24H2，Guide 称 macOS ≥ 13.0 / Windows 10 1909+;; ==**遵循README.md所述内容，CoreStartupGuide.md已经修改，等待确认**== | 版本基线混淆，CI/打包脚本无法统一判定 |
| DOC-04 | `Dev.md:27-30` | `api.md:15-16` | AI 后端分平台锁定：Dev.md 称「macOS 一律 MLX，Win/Linux 一律 GGUF」，api.md 称「OAI 兼容层完整实现...第三方客户端可零修改接入」但未说明后端选型限制;; ==**等待确认与修改**== | 第三方集成文档缺失平台约束说明 |
| DOC-05 | `Dev.md:365-372` | `api.md:253-257` | Chat Completions 扩展字段 `xijian`：Dev.md 示例含 `inject_memory`/`memory_top_k`/`guard_level`，api.md 示例仅含 `character_id`/`world_id`/`nsfw_allowed` ;;==**等待确认与修改（同时包含代码、文档）**== | 接口契约不一致，客户端无法依赖文档编写 |
| DOC-06 | `Dev.md:389-392` | `api.md:566-568` | 记忆条目字段：Dev.md 用 `attributes.{importance,decay,category}`，api.md 用 `importance`/`decay`/`category` 直接字段 + `tags` ;;==**等待确认与修改（同时包含代码、文档）**== | 数据模型文档分裂，存储/检索逻辑无法对齐 |
| DOC-07 | `Dev.md:407-418` | `api.md:694-740` | 保护模块接口：Dev.md 描述「双重确认关闭」、`/rollback`，api.md 细化为两步挑战 `challenge_id`/`challenge_phrase` + 审计导出，字段名、流程均不一致 ;;==**等待确认与修改（同时包含代码、文档）**== | 安全关键接口文档分歧，审计/合规风险 |
| DOC-08 | `CoreWinSupport.md:86-106` | `Dev. Function List功能清单v2.md:A5.4` | 过载防护指标：WinSupport 列出 CPU/内存/温度/GPU 五指标，功能清单 v2.1 表格仅保留 CPU/内存/SoC温度/GPU 四指标且阈值不同（如 CPU 严格档 93%/60s vs 95%/100s）==**详细情况有待确认，但CoreWinSupport.md现已弃置**== | 跨平台阈值文档未同步，Windows 部署时不知依据哪套 |
| DOC-09 | `Dev. Function List功能清单v2.md:C5` | `devkit/__init__.py:125-158` | SMTP 常量：清单称「硬编码常量（参见 `xijian_api/devkit/__init__.py` 顶部）」，实际常量在 `devkit/__init__.py` 且通过环境变量覆盖，清单路径错误 ;;==**以代码为准，需对文档全面修改，等待修改**== | 开发者按文档找不到配置位置 |
| DOC-10 | `Dev. Function List功能清单v2.md:C5` | `devkit/__init__.py:151-153` | 附件大小限制：清单写 `1_200_000_000`（1200 MB），代码默认 `512_000_000`（512 MB），且环境变量名不一致（`XIJIAN_DEV_MAX_BYTES` vs 清单未提） ;;==**以代码为准，需对文档全面修改，等待修改**== | 提交管线体积校验与文档不符，会误判合法包 |
| DOC-11 | `Dev. Function List功能清单v2.md:B` | `CoreWinSupport.md:74-83` | B 章 TouchBar/Dynamic Island：清单标注 `[TODO: 本章待补]`，WinSupport 单独列出「不支持 Windows 的技术原因」，但两文档对「是否实现」的结论语气不一（清单占位 vs WinSupport 明确不实现）;;==**CoreWinSupport.md已弃置，本项无需处理**== | 文档维护碎片化，新人难以判断功能状态 |
| DOC-12 | `Dev. Function List功能清单v2.md:A8` | `CoreWinSupport.md:49-71` | A8 桌宠/动态壁纸：清单 US-A8-02 称「桌宠经允许可操作电脑」，WinSupport 称 Windows 无透明穿透窗口/全局输入注入，建议仅提供「窗口化桌宠」模式，两文档对 Windows 可行性结论相反 ;;==**CoreWinSupport.md已弃置，本项无需处理**== | 产品规划与平台适配文档打架 |
| DOC-13 | `api.md:423-437` | 代码实现 | Video 生成异步接口：api.md 定义 `POST /v1/videos/generations` 返回 202 + `video.generation` 对象，实际 `routes/videos.py` 仅有同步占位存根，无任务队列、状态轮询、结果下载 ;;==**等待代码补全实现**== | API 文档描述了不存在的异步流程 |

---

## 3. A. 用户功能模块：功能清单与代码实现的差距

### A1. 记忆系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A1-01 | A1.1 受保护模块清单表 `protected_modules` / 角色关联表 `character_protected_module` / 手动备份表 `manual_backups`（AC-1/AC-3） | 无任何代码、无路由、无测试 ;;==**等待实现，优先**== | 完全缺失 |
| A1-02 | A1.1 自动备份策略：定时（每日凌晨）+ 事件触发（≥50 条手动编辑 / 首次加载 / 安全终止后）、zstd 压缩、指数退避重试 ×3 | 无定时器、无事件钩子、无 zstd 依赖、无重试逻辑 ;;==**等待实现**== | 完全缺失 |
| A1-03 | 记忆条目软删除（写 `deleted_at`，保留 7 天可恢复） | `stubs/memory.py:259` `delete()` 直接 `pop` 硬删除，`deleted_at` 从不写入 | 实现不符 |
| A1-04 | A1.2 引用审查：模型凭空捏造过去 → 必须 block 并触发重生成（最多 2 次） | `stubs/citations.py:148` `audit` 最多返回 `VERDICT_WARN`，`VERDICT_BLOCK="block"` 定义了但从不返回；`stubs/chat.py` 记录 audit 但从不 regenerate（grep "regenerat" 无命中） | 核心防幻觉机制失效 |
| A1-05 | `memory_citations` 表落库（id/response_id/entry_id/citation_kind） | 仅追加到通用 `state.audits` 列表，表结构未持久化 | 数据模型缺失 |
| A1-06 | A1.2 自动升级长期记忆（`should_promote_to_long`/`promote_to_long`） | 两函数已定义（`stubs/memory.py:322,341`），但 grep 显示从未被任何循环/定时任务调用，属死代码 | 逻辑未接线 |
| A1-07 | A1.2 遵循 per-character `force_recall_on_history` 开关 | 强制召回只检查全局 `xijian.recall.enabled`，忽略 `character_memory_config.force_recall_on_history` | 配置未生效 |
| A1-08 | A1.2 流式路径跳过强制召回管线 | `chat.stream_chunks()` 直接委托 `model_registry.complete_stream()`，未跑 `loadContext`/`recall_memory`/安全审查；仅非流式 `complete()` 走 `force_recall_pipeline()` | 流式对话绕过核心记忆与安全管线 |
| A1-09 | 接口路径/参数与清单一致：`GET /v1/characters/{cid}/memory/entries?type=&page=` | 实际路由 `GET /v1/xijian/memory/entries`（`routes/xijian_memory.py:26`），`list_entries` 未透传 `type` 过滤（stub 支持但路由不传） | 接口契约不符 |
| A1-10 | 工具结果超窗 → 摘要后回灌，原始结果存入 memory | 无实现路径 | 完全缺失 |

### A2. OpenAI 兼容的 AI 模块

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A2-01 | 角色上下文注入顺序：安全预检 → 人设(C2.4) → 记忆(A1.2) → 状态(A3.2) → 世界(A4) → 拼 system+history | `stubs/chat.py` 仅注入了记忆块 + recall 规则系统消息，**人设 / 角色状态 / 世界上下文均未注入**（notes 确认 E/F 步未接） | 核心上下文组装缺失 |
| A2-02 | 工具调用 (MCP)：工具描述注入 → 模型决定调用 → 隙间执行 → 结果回灌 | 仅 `recall_memory` 一个工具被注入，**无通用 MCP 工具描述注入 / 执行 / 结果回灌机制**，无桌面控制/文件/应用启动/浏览器自动化 | 完全缺失 |
| A2-03 | 多模态输入：图片/音频/视频片段作输入消息，模型不支持时降级为占位描述 | `stubs/chat.py:448` `_content_to_text` 把 list-content 扁平化为纯 `text`，丢弃 image/audio/video part，**无降级占位路径** | 多模态输入丢失 |
| A2-04 | 多模态支持矩阵 `[TODO: 列出每个模型后端支持的模态]` | 仍为 TODO，未补齐 | 文档占位 |
| A2-05 | 工具结果超窗 → 摘要回灌，原始结果存入 memory | 同 A1-10，无实现 | 完全缺失 |

### A3. 角色与状态系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A3-01 | A3.1 六张资源表：`character_models`/`character_motions`/`character_voices`/`character_handwritings`/`character_styles`/`character_asset_cache` | 均不存在。`stubs/characters.py` 仅存基础字段；无 VRM/FBX/GLB 多版本绑定与切换 | 完全缺失 |
| A3-02 | v2.1「彻底移除 Live2D」 | `stubs/characters.py:41,59` 仍保留 `live2d_model` 字段；无 VRM/three-vrm 加载、无贴图/动作/声音数据、无笔迹存档、无风格文档、无资产缓存 | 残留字段违反决策，新能力全无 |
| A3-03 | A3.2 状态注入对话 & `can_dialogue` 强制 | `stubs/character_state.py` 状态机/衰减/tick 线程/日志实现良好（内存态）。但**状态未注入 chat 上下文**，且健康 ≤0 时的 `can_dialogue=False`（`character_state.py:618`）未在对话路径强制（notes 确认） | 状态系统与主管线脱节 |
| A3-04 | A3.1 动作库 / 跨模态一致性 / LRU 缓存 | 无 idle/happy/sad/angry/surprised 动作库；图像/视频生成时的 `pose_image`/`motion_clip` 注入缺失；`character_asset_cache` 与 LRU 上限未定义；资源导入是假占位 zip（`stubs/resources.py:31-52`），不绑定上述模型/动作/声音 | 完全缺失 |

### A4. 模拟世界系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A4-01 | A4.1 场景生成（事件触发图像/3D 生成） | 事件引擎已实装（触发/冷却/lost_priority_race/节流），但 US-A4.1-03/AC-2「事件触发场景生成」未做：`stubs/events.py:74-78` 明确不调用图像/3D 管线，仅记 `needs_scene`/`scene_ref_id`，无生成队列、无占位图+文字降级 | 核心联动缺失 |
| A4-02 | A4.1 内置事件库 / 事件类工厂 / 单世界上限 200 条 / 触发条件冲突检测 | 无内置常见事件库；C1.1 侧「一类事件批量实例化」「单世界上限 200 条」「触发条件冲突拒绝保存」均未实现（见 C1-01） | 完全缺失 |
| A4-03 | A4.1 触发器能力：时间触发器仅 `daily`/`hourly`，无 cron/星期/月份级节日；condition 触发器字段白名单未校验，接受任意 key | `stubs/events.py` 验证器仅支持四种类型，时间触发器频率受限；condition 无字段白名单 | 能力受限 |
| A4-04 | A4.1 事件 fire 后无记忆回写（US-A4.2-04「世界内发生的事自动影响记忆」） | 事件仅入 `world_event_instances` + 广播，**不写角色记忆**；`affected_npcs` 不主动推导 | 跨模块联动缺失 |
| A4-05 | A4.2 NPC 自动生成（基于世界观文档 + 模板） | `create()` 需手工填 name/persona；`seed_default()` 注释「We do not seed any default NPCs」（`stubs/npcs.py:979-996`），未基于世界观文档 + 模板自动生成 | 完全缺失 |
| A4-06 | A4.2 NPC 自我决策（LLM 推理） | 算力调度/档位/日志表已落地，但 `tick_world` 仅标记 `last_think_at`，**无 LLM 调用、无 state 推进**（注释明言留给 operator，`stubs/npcs.py:716-805`） | 核心决策环节空转 |
| A4-07 | A4.2 算力不足降级未端到端验证 | `_should_degrade` 逻辑存在，但需 overload 模块提供 P99 延迟输入，跨模块实时联动无端到端链路测试 | 集成验证缺失 |
| A4-08 | A4.3 POI / 交通 / 世界互动系统 | `pois`/`travel_modes` 表不存在（grep 无命中）；规范 `interactions` 表（poi_id/target_type/target_id/action/effects/cooldown_sec）未实现——现有 `stubs/interactions.py` 是角色 hug/kiss 亲密互动演示，**非 A4.3 世界场景互动**；AC-2 不可回溯（无 audit 写入）、AC-3 体力扣减、影响传播均未做 | 完全缺失 |
| A4-09 | A4.4 经济系统（货币/余额/交易/非法手段/经济总系统 tick） | `world_currencies`/`wallets`/`transactions`/`world_economy_state` 四表全部不存在（无 state 键、无 stub、无 route）；货币定义、余额、上架/购买/出售、NPC 主动偷窃/诈骗、`是否允许非法手段` 开关、经济总系统 tick（通胀/流动性/季节）全未实现 | 完全缺失 |

### A5. 安全模块

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A5-01 | A5.1 OOC（人设偏离）检测 | `stubs/protection.py:23-28,118-154` 的 `guard_preview` 只匹配注入/探测关键词，**无人设文档比对，无偏离度计算**。AC-1（OOC 触发率 <1%）无法保证 | 核心检测缺失 |
| A5-02 | A5.1 危险场景例外机制 | 决策树的「场景危险 → allow_with_exception」未做；`guard_preview(context=...)` 参数被接受但从未使用，无 world 危险等级 / 事件标签检查。AC-2（例外必须显式记录原因）未满足 | 例外机制缺失 |
| A5-03 | A5.1 输入预审 / 输出后审未接入 chat 管线 | 仅有一个手动 `POST /guard/preview` 端点（`routes/xijian_protection.py:57-63`），**实时流式输出扫描、输入送模型前过滤均未接入 `stubs/chat.py`** | 管线未接入 |
| A5-04 | A5.1 数据模型 `safety_audit_log` / `safety_rules` | 无这两张表；通用 `audits` 列表形状不同，verdict 为 `safe`/`blocked` 而非 `pass|warn|block|allow_with_exception`；规则硬编码，`safety_rules` 表缺失 | 数据模型不符 |
| A5-05 | A5.1 工具调用审计 | `stubs/chat.py:420-684` 处理 tool_calls，**无 audit/guard 调用** | 审计盲区 |
| A5-06 | A5.1 审查模块崩溃 → 降级最严格档 | 边界场景「审查崩溃 → 降级最严格档」无对应逻辑 | 容错缺失 |
| A5-07 | A5.2 电脑控制防护（MCP 防护）完全未实现 | 无 MCP 监控、无危险动作黑/白名单（`mcp_action_blacklist` 表缺失）；无全局快捷键安全终止（AC-2 <200ms）；无 freeze→dump_context→确认→sanitize→reload 流程；无专用备份文件夹 / 锁定模式 / `kill -9` 边界处理 | 完全缺失 |
| A5-08 | A5.3 自动快照部分缺失 | `safety_snapshots` 按清单 schema（scope/target_id/reason 枚举/expires_at）未实现，`snapshot()` 用 ad-hoc 形状（`stubs/protection.py:160-180`）；无 `backup_policies` 表、无 `max_total_bytes` 上限与超限提示；无 zstd 压缩（≥0.4）与用户同意压缩流程；无每小时定时自动快照（仅按需/过载时） | 关键字段与流程缺失 |
| A5-09 | A5.4 过载 action handler 仍有 3 个无外部调用方 | `degrade_tts` / `compress_memory` / `emergency_dump` 仅 `register_action_handler` 被 `ACTION_SUSPEND_IDLE_NPCS` 调用（`stubs/npcs.py:962-971`），另三个无调用方，等 A6/A1.2/A1.1 起来时调用 | 集成未完成 |

### A6. 实时通话

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A6-01 | 全双工语音流（基于 STT/TTS） | 仅一次性 REST `/v1/audio/speech` 与 `/v1/audio/transcriptions`（`audio.py:14,47`）；**无双向 WS 语音路由、无 barge-in、无流式 STT/TTS**（STT 后端为批处理） | 完全缺失 |
| A6-02 | `voice_calls` / `call_events` 表 | grep 全仓库（排除 macapp）无这两张表 | 数据模型缺失 |
| A6-03 | 模型动作联动（VRM 骨骼/BlendShape） | 运行时按情感/语义触发 motion 的代码缺失；motion 仅在 `devkit/motion_editor.py` 离线编辑器存在，**无运行时通话触发** | 完全缺失 |
| A6-04 | 通话特效/动画 | 角色配置触发的特效/简短动画无代码 | 完全缺失 |
| A6-05 | DiffSinger 歌声未接入通话 | `DiffSingerEngine`（`devkit/tts_engine.py:487`）仅为独立 dev TTS 引擎，**未接入任何通话/唱歌管线** | 完全缺失 |

### A7. 主动发起聊天或通话（仅 macOS/iOS）

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A7-01 | `character_initiated_actions` 表 | grep 无命中 | 数据模型缺失 |
| A7-02 | 主动决策逻辑（基于角色配置 [TODO: 主动频率上限] 与状态） | 仅 `ws_routes.py:207-217` 一个硬编码 demo 在连接 3s 后推固定消息 `char_yuki`，**无频率上限、无配置/状态驱动决策** | 仅占位实现 |
| A7-03 | 用户拒绝后「理解」写回记忆 | AC-2 未做 | 完全缺失 |
| A7-04 | 全局 / 按角色开关 | 非 macapp 代码中无开关；系统通知 / 来电接听 UI 属 macapp（排除） | 完全缺失 |

### A8. 桌宠 / 动态壁纸（仅 macOS）

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A8-01 | `desktop_pets` / `dynamic_wallpapers` / `pet_action_log` 表 | grep 无命中 | 数据模型缺失 |
| A8-02 | 桌宠可审计日志 | `pet_action_log` 缺失；仅 `ws_routes.py:258` 一个 no-op echo（`desktop_pet.command.echo`）+ `:256` 的 `emergency_pause` ack，**无持久化** | 完全缺失 |
| A8-03 | 壁纸模式写操作禁用强制 | AC-4 未做；权限矩阵无 macapp 外强制代码 | 完全缺失 |

---

## 4. B. Apple TouchBar & Dynamic Island

| 编号 | 问题 | 详述 |
|------|------|------|
| B-01 | 整章为 `[TODO: 本章待补]`（清单第 1295 行标记待补） | 非 macapp 代码无任何 `touchbar`/`dynamic_island` 实现 ;;==**暂不处理，等待项目首个正式版发布后，将本项列入开发计划**== |

---

## 5. C. Development Kit (开发者工具)

| 编号 | 问题 | 详述 | 证据 |
|------|------|------|------|
| C0-01 | DevKit 包位置与清单不符 | 清单要求位于 `core/xijian_api/devkit/`；实际为顶层 `devkit/` 包，`api.py`/`__init__.py` 在其内。独立性满足，但位置偏离（硬编码 SMTP 常量也在 `devkit/__init__.py` 顶部，非清单所述 `core/xijian_api/devkit/__init__.py`） ;;==**修改文档要求，以当前实际位置为准**== | `devkit/Problems.md:129` |
| C1-01 | C1.1 事件类工厂 / 上限 / 冲突检测未实现 | DSL 编辑器与校验已做，但「一类事件批量实例化」「单世界上限 200 条」「触发条件互相冲突 → 拒绝保存」未实现（关联 A4-02） | `devkit/Problems.md:130` |
| C2-01 | C2.1 文本生成声音为桩 | `generate_voice_from_text`（`voice_cloner.py:201-205`）直接 HTTP 501「语音合成（MeloTTS）功能仍在制作中」；`MeloTTSEngine` 存在却未接入此路径，AC-2（可试听可调）不满足 ;;==**等待修改文档（部分细节需要修改）与代码实现，高优先级**== | `devkit/Problems.md:131` |
| C2-02 | C2.1 声音克隆不克隆且跳过版权确认 | `clone_voice_from_file`（`voice_cloner.py:228-283`）仅 `save_voice` 存参考文件，**无真实说话人适配**，且 AC-1（克隆前版权确认）未做 ;;==**等待实现声音克隆，同时，从文档和代码中彻底删除版权克隆有关内容**== | `devkit/Problems.md:132` |
| C2-03 | C2.1 TTSManager 引用未定义属性（疑似 Bug） | `get_engine`/`list_all_voices` 引用 `self._engines`，但仅定义 `self._tts_engines`/`self._singing_engines`（`tts_engine.py:694,697,704`），任意 `synthesize()` 抛 `AttributeError` ;;==**等待确认与修复**== | `devkit/Problems.md:133` |
| C2-04 | C2.7 微调 / prompt 蒸馏为桩 | `start_finetuning_job`/`export_finetuning_dataset`（`dialog_editor.py:201-248`）返回占位 dict，无真实训练/导出 ;;==**需要及时确认和修复**== | `devkit/Problems.md:134` |
| C2-05 | C2.8 AI 生成 VRM 非真实 | `generate_model_from_text`（`model_viewer.py:264-327`）要么下载整个任意 HF repo，要么写占位 `.glb` JSON（"Placeholder — full AI VRM generation requires MLX backend"）；无文本+参考图→VRM 1.0 生成或骨骼绑定 ;;==**暂时不处理，等待项目首个正式版即将发布时再将此项列入开发计划，低优先级**== | `devkit/Problems.md:135` |
| C2-06 | C2.8 FBX 导入不支持 | `register_model` 直接拒绝 `.fbx`（`fbx_conversion_unavailable`，`model_viewer.py:55-64`）；转换仅能调外部 `UNITY_PATH`/`BLENDER_PATH` CLI，清单将 `.fbx` 列为支持导入格式 ;;==**等待确认与代码功能实现**== | `devkit/Problems.md:136` |
| C2-07 | C2.8 VRM 1.0 校验仅部分 | `validate_model_format`（`model_viewer.py:140-185`）只查 glTF JSON 中 `VRM`/`VRMC_vrm` 扩展标记，**未校验完整 VRM 1.0 规范**；AC-3 模型大小上限（<50MB/<20MB）保存时不强制，仅记录 `size_bytes` ;;==**等待补全**== | `devkit/Problems.md:137` |
| C2-08 | C2.9 关键帧编辑 / 回放未实现 | 关键帧仅存 dict（`motion_editor.py:396-424`），无真实动画曲线编辑；回放未实现，正确性仅依赖骨骼名比对（`validate_motion_skeleton`）与外部 `bvh2vrm` CLI ;;==**等待确认，并补全功能，中优先级**== | `devkit/Problems.md:138` |
| C3-01 | C3 剧情无执行/模拟引擎 | `plot_editor` 仅持久化 `nodes.json`/`edges.json`（`plot_editor.py:96-149`），无「被模拟世界读取并执行」的 simulate/execute，AC-1/AC-2 绑定强制仅为数据层、未校验运行 ;;==**等待确认与补全**== | `devkit/Problems.md:139` |
| C4-01 | C4 AI 设计辅助整体不可用 | `auto_suggest`/`suggest_with_questions`（`ai_assistant.py:133,144-148,207-213`）返回「暂不开放，请耐心等待」，真实 LLM 调用回退静态模板；US-C4-01/02、AC-2（AI 必须优先询问用户）未满足 ;;==**等待项目首个正式版发布后再将本项列入开发计划**== | `devkit/Problems.md:140` |
| C4-02 | C4 `dev_ai_assist_log` 表未实现 | 用 JSON `assist_log.json` 近似，`source='ai_suggested'` 标记未跨 AI 产出字段强制；`ai_ratio` 为每事件代理值或直读调用方 payload，非清单的「字段计数」算法（`ai_assistant.py:98-111`,`devkit/__init__.py:832`） ;;==**等待确认，并将此项列入低优先级开发计划，当前暂不做明确处理**== | `devkit/Problems.md:141` |
| C5-01 | C5 7z 调用形式与清单字面不符 | 清单写 `py7zr.SevenZipFile(mode='solid')`；实际 `pack_payload` 用 `SevenZipFile(target, mode="w", solid=True)`（`devkit/__init__.py:505`）。功能等价固实，但非文档 API 形式 ;;==**本项无需处理，可忽略**== | `devkit/Problems.md:142` |

---

## 6. API 文档与实际路由的不一致 ;;==**需要及时确认，并处理：要么改文档，要么改代码**==

| 编号 | API 文档 (api.md) | 实际路由 (core/xijian_api/routes/) | 差异 |
|------|-------------------|-----------------------------------|------|
| API-01 | `GET /v1/characters/{cid}/memory/entries?type=&page=` | `GET /v1/xijian/memory/entries` | 命名空间、参数透传均不符 |
| API-02 | `PATCH/DELETE /v1/entries/{eid}` | `/v1/xijian/memory/entries/<id>` | 路径前缀、参数名不符 |
| API-03 | `GET /v1/xijian/characters/{character_id}/state` (v1 字段: affection/mood/recent_memory_summary) | 实际返回 A3.2 数值字段 + 状态机字段 | 文档未同步 v2 字段 |
| API-04 | `POST /v1/xijian/characters/{character_id}/state` | 实际路由同，但保护检查逻辑不同 | 保护模块集成不一致 |
| API-05 | `POST /v1/xijian/worlds/{world_id}/transition` | 实际 `routes/xijian_worlds.py` 有 `transition` 但字段不同 | 字段不符 |
| API-06 | `GET /v1/xijian/worlds/{world_id}/state` 返回经济/健康/饮食/体力/心智 | 实际 `worlds.get_state()` 返回 environment + compute_config + npc_count | 返回结构完全不同 |
| API-07 | `POST /v1/xijian/protection/disable` 两步挑战 | 实际 `routes/xijian_protection.py` 仅有 `/guard/preview`，无 disable 端点 | 核心安全接口缺失 |
| API-08 | `POST /v1/xijian/memory/search` 向量检索 | 实际 `routes/xijian_memory.py` 仅有关键词搜索 | 向量检索未实现 |
| API-09 | `POST /v1/xijian/memory/consolidate` / `forget` | 无对应路由 | 接口缺失 |
| API-10 | `GET /v1/xijian/protection/snapshots` / `rollback` | 无对应路由 | 接口缺失 |
| API-11 | `POST /v1/xijian/protection/audit/export` | 无对应路由 | 接口缺失 |
| API-12 | `POST /v1/chat/abort` / `POST /v1/xijian/generation/abort` | 无对应路由 | 取消机制缺失 |
| API-13 | WebSocket 事件类型表（5.4） | `ws_routes.py` 仅处理 `desktop_pet.command.echo`/`emergency_pause`，缺失 `character.proactive_message`/`world.event_occurred`/`memory.consolidated`/`protection.alert`/`generation.progress` 等核心事件 | WS 事件体系不完整 |

---

## 7. 配置与部署文档的不一致

| 编号 | 文档 | 配置项 | 文档值 | 代码/实际默认值 | 影响 |
|------|------|--------|--------|-----------------|------|
| CFG-01 | `CoreStartupGuide.md:122` | `overload.tier` | `"medium"` | 代码 `stubs/overload.py` 仅接受 `strict`/`medium`，但默认读取逻辑未明确 | 部署时不知实际生效档位 |
| CFG-02 | `CoreStartupGuide.md:106` | `auth.token_file` | `/var/lib/xijian/xijian-{pid}.token` | `core/xijian_api/auth.py` 生成在 `/tmp/xijian-{pid}.token` | 生产环境 token 重启丢失，握手失败 |
| CFG-03 | `CoreStartupGuide.md:109` | `storage.base_dir` | `/var/lib/xijian` | 无统一存储目录，各模块自行用临时目录或内存 | 数据分散、无法备份 |
| CFG-04 | `Dev. Function List功能清单v2.md:C5` | `DEV_SUBMIT_MAX_ATTACHMENT_BYTES` | `1_200_000_000` (1200 MB) | `devkit/__init__.py:151` 默认 `512_000_000` (512 MB) | 提交体积校验与文档不符 |
| CFG-05 | `CoreWinSupport.md:103` | 过载防护 Windows 仅启用 CPU+内存 | 建议暂仅启用双指标 | `stubs/overload.py` 无平台分支，仍尝试读取温度/GPU 指标（macOS `powermetrics`） | Windows 运行报错或指标异常 |
| CFG-06 | `api.md:26` | 鉴权：`Bearer Token`，仅 `127.0.0.1` 访问 |  | `CoreStartupGuide.md:17` 默认监听 `0.0.0.0`，**允许外部访问** | 安全基线冲突 |

---

## 8. 测试与质量保障缺口

| 编号 | 问题 | 详述 |
|------|------|------|
| TEST-01 | 核心功能无集成测试 | `core/tests/` 仅有单元测试（stub 级），缺乏跨模块集成测试（如 A1.2 记忆→A2 聊天管线、A4.1 事件→A1 记忆回写、A5.4 过载→A4.2 NPC 调度） |
| TEST-02 | 保护模块覆盖率不足 | `Dev.md:611` 要求保护模块 90% 行覆盖率，实际 `stubs/protection.py` 核心分支（OOC/例外/崩溃降级）无测试覆盖 |
| TEST-03 | AI 抽象层无 mock backend 测试 | `Dev.md:612` 要求 AI 抽象层有 mock backend 测试，实际 `ai/backends/mock.py` 仅为占位，无契约测试 |
| TEST-04 | 跨平台握手流程无集成测试 | `Dev.md:613` 要求有集成测试跑完整握手，实际无 |
| TEST-05 | 无端到端性能基线测试 | `Dev.md §10` 定义了 TTFT/内存增长/渲染帧率基线，但 CI 中无性能回归测试 |
| TEST-06 | 无安全审计测试 | A5.1/A5.2 关键安全模块无注入攻击/OOC 绕过/越权操作的红队测试用例 |

---

## 总结统计

| 类别 | 问题数 | 关键/阻断级 | 主要未实现区域 |
|------|--------|-------------|----------------|
| 核心架构 | 5 | 5 | 无持久化、无 MCP、AI 后端选型未落地、嵌入模型缺失、服务层缺失 |
| 文档不一致 | 13 | 3 | 安全基线冲突、设备规格冲突、接口契约分裂、跨平台阈值不同步 |
| A1 记忆系统 | 10 | 4 | 备份系统全无、软删除/引用审查/自动升级/流式管线缺失 |
| A2 AI 模块 | 5 | 3 | 上下文注入不全、MCP 缺失、多模态输入丢失 |
| A3 角色状态 | 4 | 3 | 资源表全无、Live2D 残留、状态未注入对话 |
| A4 模拟世界 | 9 | 5 | 场景生成/内置事件库/NPC 自动生成/自我决策/POI/经济系统全无 |
| A5 安全模块 | 9 | 5 | OOC 检测/例外机制/管线接入/数据模型/MCP 防护/快照/handler 集成均缺失 |
| A6 实时通话 | 5 | 5 | 全双工/数据表/动作联动/特效/歌声全无 |
| A7 主动发起 | 4 | 3 | 数据表/决策逻辑/拒绝记忆/开关全无 |
| A8 桌宠/壁纸 | 3 | 2 | 数据表/审计日志/写禁用全无 |
| B Apple 专属 | 1 | 1 | 整章 TODO |
| C 开发者工具 | 14 | 4 | TTS/克隆/VRM/FBX/剧情执行/AI 辅助/日志算法/包位置等核心功能为桩 |
| API 文档不符 | 13 | 3 | 命名空间/参数/返回结构/核心接口缺失/WS 事件缺失 |
| 配置部署不符 | 6 | 2 | token 路径/存储目录/体积限制/过载指标/监听地址冲突 |
| 测试质量缺口 | 6 | 2 | 无集成/无安全/无性能/无跨平台握手测试 |
| **合计** | **~107** | **~45** | |

> **说明**：`devkit/Problems.md` 统计约 67 个缺口（已实装项不计入），本报告在其基础上结合全量文档与代码补充了文档间不一致、API 契约不符、配置部署冲突、测试缺口等约 40 项，合计约 107 个问题，其中关键/阻断级约 45 个。最核心的阻断项集中在：**无持久化存储、无 MCP、AI 后端选型未落地、安全模块管线未接入、模拟世界核心子系统（经济/POI/NPC决策）全缺失、实时通话全缺失**。建议优先解决 ARCH-01/02/03/04、A5-01/03/07、A4-08/09、A6-01/02、API-07/12 等阻断项。

---

## 附录：检查方法与范围确认

1. **文档扫描**：读取 `docs/` 下所有 `.md` 文件（排除 `已弃置/`），共 9 份现行文档。
2. **代码扫描**：遍历 `core/xijian_api/`、`devkit/` 目录下所有 `.py` 文件（排除 `__pycache__`、`.venv`、测试文件），重点对比 `stubs/` 实现与 `routes/` 暴露接口。
3. **基准文档**：以 `docs/Dev. Function List功能清单v2.md` (v2.5, 2026-07-11) 为唯一功能权威来源，逐节核对 AC-*、US-*、接口定义、数据模型表、Mermaid 流程。
4. **已知问题库**：参考 `devkit/Problems.md`（2026-07-11 盘账）作为已确认缺口基线，在此基础上补全文档间不一致、API 契约不符、配置冲突、测试缺口。
5. **排除范围**：`macapp/` 目录（macOS 原生 App 代码）、`docs/已弃置/` 目录、第三方依赖代码（`core/.venv/`、`devkit/.venv/`）。
6. **输出**：本报告 `docs/chk20260715.md`，按模块分类、标注证据位置、给出缺口分类（完全缺失/实现不符/文档占位/集成未完成）。

---