# 隙间项目文档与代码一致性检查报告（chk20260728）

> 检查日期：2026-07-28
> 检查范围：`docs/`（排除 `已弃置/`、`md2pdf/`）、`core/`（排除 `core/.venv/`）、`devkit/`（排除 `macapp/`、`devkit/.venv/`）
> 参考基准：`docs/Dev. Function List功能清单v2.md`（v2.9，2026-07-22）
> 上一份报告：`docs/chk20260715.md`（v2.5，2026-07-15）
> 测试环境：conda 环境 `xijianBase`，`pytest` 1667/1667 通过（0 失败、0 跳过）

---

## 目录

1. [总体结论](#1-总体结论)
2. [自 chk20260715 以来已落地的章节](#2-自-chk20260715-以来已落地的章节)
3. [仍未完整实现的功能（按章节）](#3-仍未完整实现的功能按章节)
4. [实现错误](#4-实现错误)
5. [文档之间的冲突](#5-文档之间的冲突)
6. [测试结果](#6-测试结果)
7. [与上一份报告（chk20260715）的对比](#7-与上一份报告chk20260715的对比)
8. [附录：检查方法](#8-附录检查方法)

---

## 1. 总体结论

自 2026-07-15 至 2026-07-28，隙间项目按 v2 功能清单新增了 6 个章节的 stub + 路由 + 测试：

| 章节 | 状态 | 备注 |
|------|------|------|
| A4.1 事件调度接入 | 已落地 | v2.4，117 个新测试 |
| A4.3 场景与互动 | 已落地 | v2.5，148 个新测试 |
| A4.4 经济系统 | 已落地 | v2.6，282 个新测试 |
| A5.1 输出审查 | stub+路由已落地 | v2.7，150 个新测试；**未接入 chat 管线** |
| A5.2 MCP 防护 | stub+路由已落地 | v2.8，262 个新测试；server 侧已接 MCP `call_tool` gate，**未接全局快捷键 / 客户端弹窗** |
| A5.3 自动世界/记忆上下文备份 | 已落地 | v2.9，105 个新测试；用 `zlib` 替代 `zstd`，未起定时 tick 线程 |

测试总数从 762（2026-07-11 基线）涨到 **1667**，0 回归。

但是：

- **核心 chat 管线（A2）尚未接入新章节**——A5.1 `scan_input/scan_output` 没接、A1.2 流式路径仍跳过 recall、A3.2 状态未注入、A1.1 自动备份系统未起、A4.2 NPC 自我决策仍空转。
- **核心持久化层仍未起**——所有 stub 仍是进程内 `state.*` dict，无 SQLite、无 `sqlite3`/`sqlalchemy` 依赖、进程重启即丢。
- **A6/A7/A8 完全未起**（实时通话、主动发起、桌宠/壁纸）。
- **API 文档（`api.md`）严重滞后**：自 v2.4 起新增的 `/v1/xijian/{events,scenes,economy,currencies,wallets,safety,mcp,backups,overload}/*` 等 100+ 路由在 `api.md` 中**完全未文档化**，仅在 `openapi.yaml` 中存在。
- **多处配置 / 战略定位文档冲突未解决**（详见 §5）。

报告统计：

| 类别 | 数量 |
|------|------|
| 仍未实现的功能项 | 56 |
| 实现错误 | 8 |
| 文档间冲突 | 12（其中 CRITICAL 2、HIGH 4、MEDIUM 6） |
| 合计待处理项 | 76 |

---

## 2. 自 chk20260715 以来已落地的章节

### 2.1 A4.1 事件调度接入（v2.4，2026-07-10）

- `stubs/events.py`（978 行）+ `routes/xijian_events.py`（302 行）原已存在，本轮接入到 `seed_all()`、可选路由表、117 个测试。
- 已实装：4 类触发器（time/interval/probability/condition）、风暴节流（`DEFAULT_GLOBAL_COOLDOWN_SECONDS=60`）、优先级竞争（`lost_priority_race`）、过载短路、WS 广播 `event.fired`。
- 证据：[events.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/events.py)、[xijian_events.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_events.py)

### 2.2 A4.3 场景与互动（v2.5，2026-07-11）

- 新建 `stubs/pois.py`（490 行，三级父子约束）、`stubs/travel_modes.py`（330 行，`estimate_trip` 预演）、`stubs/scene_interactions.py`（680 行，`trigger()` 含 cooldown / character-state gate / NPC 死亡 gate / audit / A4.1 联动）。
- 路由统一挂 `/v1/xijian/scenes/*`，148 个新测试。
- 证据：[pois.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/pois.py)、[travel_modes.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/travel_modes.py)、[scene_interactions.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/scene_interactions.py)

### 2.3 A4.4 经济系统（v2.6，2026-07-15）

- 新建 5 个 stub：`world_currencies.py`、`wallets.py`、`transactions.py`、`world_economy_state.py`、`economy.py`（orchestrator）。
- 4 个 trade verb + 2 个 crime verb（含 30s per-NPC cooldown、确定性 hash 概率、A5.4 过载短路、`allow_illegal` 政策 gate）。
- 路由挂 `/v1/xijian/{economy,currencies,wallets}/*`，282 个新测试。
- 证据：[economy.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/economy.py)、[xijian_economy.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_economy.py)

### 2.4 A5.1 输出审查（v2.7，2026-07-19）

- 新建 `stubs/safety_rules.py`（332 行，3 种 rule_kind / severity 1-5）、`stubs/safety.py`（567 行）。
- 决策树：`scan_input` injection 必 block / forbidden word 按 severity 分 warn/block/hard_block；`scan_output` OOC 在 `world.is_dangerous` + `event_tags ⊇ {dangerous, danger, extreme, fatal, catastrophic}` 时转 `allow_with_exception`（双信号必须同时满足）；self-crash fallback 为 `hard_block`。
- 路由挂 `/v1/xijian/safety/*`，150 个新测试。
- **未接 chat 管线**：`stubs/chat.py` 中 `grep "scan_input|scan_output|safety_stub"` 0 命中（见 [chat.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py)）。
- 证据：[safety.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/safety.py)、[safety_rules.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/safety_rules.py)

### 2.5 A5.2 MCP 防护（v2.8，2026-07-20）

- 新建 `stubs/mcp_rules.py`（396 行，8 种 action_kind）、`stubs/mcp.py`（~900 行，三大子系统：Gate `check()` 7 分支 / Safety-stop 状态机 6 状态 / Snapshots dump+sanitize+restore）。
- 3 次 60s 内连续 safety_stop → `lockout`，409 拒绝后续直到 `clear_lockout` 冷重启重置。
- `sanitize_snapshot` 复用 A5.1 `forbidden_word` 规则做字段级 scrub。
- 路由挂 `/v1/xijian/mcp/*`，262 个新测试。
- **server 侧已接 chat 管线**：[chat.py:_execute_mcp_tool_call](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py#L814-L866) 通过 `xijian_api.mcp.registry.call_tool()` 触发 `ToolGateError`。
- **未接**：AC-2 "安全终止响应延迟 < 200ms"、全局快捷键 ⌃⌥⌘Q / Win+Alt+Shift+Q、客户端 UI 弹窗、`kill -9` MCP 进程——这些属于 Pywebview 客户端范畴。
- 证据：[mcp.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/mcp.py)、[mcp_rules.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/mcp_rules.py)

### 2.6 A5.3 自动世界/记忆上下文备份（v2.9，2026-07-22）

- 新建 `stubs/snapshots.py`（828 行）：核心 + 政策 + 容量 + 压缩 + 过期清理。
- 4 种 reason（`scheduled`/`overload`/`safety_stop`/`manual`）、4 种 scope（`world`/`memory`/`character`/`mixed`）、单条 500 MiB 上限、政策默认 5 GiB / 1h 间隔 / 0.7 压缩目标 / 自动压缩。
- 容量超限返 409 + `action=prompt`，操作者响应 `compress`/`drop`/`force` 三分支。
- 路由挂 `/v1/xijian/backups/*`，105 个新测试。
- A5.4 → A5.3 跨章联动已落地（过载触发时双写 A5.3 `reason=overload`）。
- **未接**：真实 zstd（用 zlib 模拟）、定时 tick 线程（默认关）、A5.2 safety_stop confirm → A5.3 双写。
- 证据：[snapshots.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/snapshots.py)、[xijian_backups.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_backups.py)

---

## 3. 仍未完整实现的功能（按章节）

### 3.1 核心架构层面

| 编号 | 问题 | 详述 | 证据 |
|------|------|------|------|
| ARCH-01 | **完全没有 SQLite 持久化** | 所有 stub 仍是 `state.*` 进程内 dict，进程重启即丢；全仓库无 `sqlite3`/`sqlalchemy`/`CREATE TABLE`。附录 A 列出约 40 张表全部未落库。 | [state.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/state.py) |
| ARCH-02 | **AI 后端引擎与 v2.1 选型不符** | `ai/backends/{mlx,gguf}` 是对 `mlx_lm`/`llama_cpp`/`mlx_audio`/`stable_diffusion_cpp` 等可选外部库的封装；库与权重不随仓库发布，未安装时 `is_available()=False` 返回 503。`mlx_video`/`stable_diffusion_cpp_video` 库不存在，永远 503。MeloTTS / DiffSinger / bge-m3 / Qwen2.5-7B 选型仅在文档与 `devkit/tts_engine.py` 中出现，未真正接入 `ai/backends/`。 | [backends/](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/ai/backends) |
| ARCH-03 | **指定嵌入模型 bge-m3 未接入** | `memory_entries.embedding` 字段在 `stubs/memory.py:175` 注释掉；`recall_search()` 用子串匹配 + 词重叠评分（非向量检索），与 A1.2 embedding 检索、A2 bge-m3 选型不符。 | [memory.py:357-373](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/memory.py#L357-L373) |
| ARCH-04 | **核心服务层缺失** | `Dev.md §3` 规划 `core/services/{character,interaction,world,memory,protection,notifier}`、`core/ai/`、`core/store/`、`core/models/` 等目录，实际仓库无这些目录，业务逻辑分散在 `stubs/` 与 `routes/` 中。 | 项目目录结构 |

### 3.2 A1 记忆系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A1-01 | A1.1 受保护模块清单表 `protected_modules` / 角色关联表 `character_protected_module` / 手动备份表 `manual_backups`（AC-1/AC-3） | 无任何代码、无路由、无测试。A5.3 的 `snapshots.py` 提供了备份存储能力，但 A1.1 的"受保护模块清单"概念未落表 | 完全缺失 |
| A1-02 | A1.1 自动备份策略：定时（每日凌晨）+ 事件触发（≥50 条手动编辑 / 首次加载 / 安全终止后）、zstd 压缩、指数退避重试 ×3 | 无定时器、无事件钩子；A5.3 用 `zlib` 替代 `zstd`；A5.3 默认 `backup_interval_seconds=3600` 但**未起后台 tick 线程** | 完全缺失 |
| A1-03 | 记忆条目软删除（写 `deleted_at`，保留 7 天可恢复） | [memory.py:179](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/memory.py#L179) 有 `deleted_at` 字段定义，但 `delete()` 直接 `pop` 硬删除，不写 `deleted_at` | 实现不符 |
| A1-04 | A1.2 引用审查：模型凭空捏造过去 → 必须 block 并触发重生成（最多 2 次） | [citations.py:90-92](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/citations.py#L90-L92) 定义了 `VERDICT_BLOCK`，但 `audit()` 最多返回 `VERDICT_WARN`；`stubs/chat.py` 记录 audit 但**从不 regenerate**（`grep "regenerat" chat.py` 0 命中） | 核心防幻觉机制失效 |
| A1-05 | `memory_citations` 表落库（id/response_id/entry_id/citation_kind） | 仅追加到通用 `state.audits` 列表，表结构未持久化 | 数据模型缺失 |
| A1-06 | A1.2 自动升级长期记忆（`should_promote_to_long`/`promote_to_long`） | 两函数已定义（[memory.py:322,341](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/memory.py#L322)），但**从未被任何循环/定时任务调用**，属死代码 | 逻辑未接线 |
| A1-07 | A1.2 遵循 per-character `force_recall_on_history` 开关 | 强制召回只检查全局 `xijian.recall.enabled`，忽略 `character_memory_config.force_recall_on_history` | 配置未生效 |
| A1-08 | A1.2 流式路径接入强制召回管线 | [chat.py:stream_chunks](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py#L1203-L1255) 直接委托 `backend.chat()`，**不跑 `loadContext`/`recall_memory`/安全审查**；仅非流式 `complete()` 走 `force_recall_pipeline()` | 流式对话绕过核心记忆与安全管线 |
| A1-09 | 接口路径与清单一致：`GET /v1/characters/{cid}/memory/entries?type=&page=` | 实际路由 `GET /v1/xijian/memory/entries`（[xijian_memory.py:26](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_memory.py#L26)），`list_entries` 未透传 `type` 过滤 | 接口契约不符 |
| A1-10 | 工具结果超窗 → 摘要后回灌，原始结果存入 memory | 无实现路径 | 完全缺失 |

### 3.3 A2 OpenAI 兼容的 AI 模块

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A2-01 | 角色上下文注入顺序：安全预检 → 人设(C2.4) → 记忆(A1.2) → 状态(A3.2) → 世界(A4) → 拼 system+history | [chat.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py) 仅注入了记忆块 + recall 规则系统消息，**人设 / 角色状态 / 世界上下文 / A5.1 安全预审均未注入** | 核心上下文组装缺失 |
| A2-02 | 工具调用 (MCP)：工具描述注入 → 模型决定调用 → 隙间执行 → 结果回灌 | MCP 工具描述注入已实现（[chat.py:_build_oai_tools](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py#L759-L811)），执行通过 `call_tool()` 走 A5.2 gate（已接）。**但仅支持 `recall_memory` 一个内置工具**，桌面控制/文件/应用启动/浏览器自动化等通用 MCP 工具未实装 | 部分缺失 |
| A2-03 | 多模态输入：图片/音频/视频片段作输入消息，模型不支持时降级为占位描述 | [chat.py:_content_to_text](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py) 把 list-content 扁平化为纯 `text`，**丢弃 image/audio/video part，无降级占位路径**（`[image]`/`[audio]`/`[video]` 占位符未实现） | 多模态输入丢失 |
| A2-04 | 多模态支持矩阵 `[TODO: 列出每个模型后端支持的模态]` | 仍为 TODO，未补齐 | 文档占位 |
| A2-05 | 工具结果超窗 → 摘要回灌，原始结果存入 memory | 同 A1-10，无实现 | 完全缺失 |
| A2-06 | 流式首字节延迟 < 1200ms（本地）/ < 3s（云端） | 无性能基线测试，无法验证 | 测试缺失 |

### 3.4 A3 角色与状态系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A3-01 | A3.1 六张资源表：`character_models`/`character_motions`/`character_voices`/`character_handwritings`/`character_styles`/`character_asset_cache` | 均不存在。`stubs/characters.py` 仅存基础字段 | 完全缺失 |
| A3-02 | v2.1「彻底移除 Live2D」 | [characters.py:40,59,83](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/characters.py#L40) 仍保留 `live2d_model` 字段；无 VRM/three-vrm 加载、无贴图/动作/声音数据、无笔迹存档、无风格文档、无资产缓存 | 残留字段违反决策，新能力全无 |
| A3-03 | A3.2 状态注入对话 & `can_dialogue` 强制 | `stubs/character_state.py` 状态机/衰减/tick 线程/日志实现良好。但**状态未注入 chat 上下文**（`grep "character_state\|cs_stub" chat.py` 0 命中），且健康 ≤0 时的 `can_dialogue=False`（[character_state.py:625-628](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/character_state.py#L625-L628)）未在对话路径强制 | 状态系统与主管线脱节 |
| A3-04 | A3.1 动作库 / 跨模态一致性 / LRU 缓存 | 无 idle/happy/sad/angry/surprised 动作库；图像/视频生成时的 `pose_image`/`motion_clip` 注入缺失；`character_asset_cache` 与 LRU 上限未定义；资源导入是假占位 zip（[resources.py:31-52](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/resources.py#L31-L52)），不绑定上述模型/动作/声音 | 完全缺失 |

### 3.5 A4 模拟世界系统

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A4-01 | A4.1 场景生成（事件触发图像/3D 生成） | 事件引擎已实装，但 US-A4.1-03/AC-2「事件触发场景生成」未做：[events.py:74-78](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/events.py#L74-L78) 明确不调用图像/3D 管线，仅记 `needs_scene`/`scene_ref_id`，无生成队列、无占位图+文字降级 | 核心联动缺失 |
| A4-02 | A4.1 内置事件库 / 事件类工厂 / 单世界上限 200 条 / 触发条件冲突检测 | 无内置常见事件库（`seed_default` 仅启动 scheduler，不 seed 任何事件）；C1.1 侧「一类事件批量实例化」「单世界上限 200 条」「触发条件冲突拒绝保存」均未实现 | 完全缺失 |
| A4-03 | A4.1 触发器能力：时间触发器仅 `daily`/`hourly`，无 cron/星期/月份级节日；condition 触发器字段白名单未校验 | [events.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/events.py) 验证器仅支持四种类型，时间触发器频率受限；condition 无字段白名单 | 能力受限 |
| A4-04 | A4.1 事件 fire 后无记忆回写（US-A4.2-04「世界内发生的事自动影响记忆」） | 事件仅入 `world_event_instances` + 广播，**不写角色记忆**；`affected_npcs` 不主动推导 | 跨模块联动缺失 |
| A4-05 | A4.2 NPC 自动生成（基于世界观文档 + 模板） | `create()` 需手工填 name/persona；`seed_default()` 注释「We do not seed any default NPCs」（[npcs.py:979-996](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/npcs.py#L979-L996)），未基于世界观文档 + 模板自动生成 | 完全缺失 |
| A4-06 | A4.2 NPC 自我决策（LLM 推理） | 算力调度/档位/日志表已落地，但 `tick_world` 仅标记 `last_think_at`，**无 LLM 调用、无 state 推进**（注释明言留给 operator，[npcs.py:716-805](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/npcs.py#L716-L805)） | 核心决策环节空转 |
| A4-07 | A4.2 算力不足降级未端到端验证 | `_should_degrade` 逻辑存在，但需 overload 模块提供 P99 延迟输入，跨模块实时联动无端到端链路测试 | 集成验证缺失 |
| A4-08 | A4.3 POI / 交通 / 世界互动系统 | 已实装（见 §2.2） | 已落地 |
| A4-09 | A4.4 经济系统 | 已实装（见 §2.3），但 `economy.py:_probability_hit` 用 `hash()` 而非 sha256，受 `PYTHONHASHSEED` 影响（见 §4 实现错误 E-01） | 已落地，存在实现错误 |
| A4-10 | A4.4 商品 / 库存系统 | spec §A4.4 提了"商品上架/购买/出售"，但 SQL schema 里没有 goods/items 表，orchestrator 也没有商品入口 | 设计决策待定 |
| A4-11 | A4.4 "经济系统崩溃" 触发世界重置 | `tick()` 函数会写 `inflation_rate`，但**没监控**这个值（什么算"崩溃"未定义） | 完全缺失 |

### 3.6 A5 安全模块

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A5-01 | A5.1 OOC（人设偏离）检测 | `stubs/safety.py` 的 `scan_output` 已实现 OOC 模式匹配，但**未接 chat 管线**——`grep "scan_input\|scan_output\|safety_stub" chat.py` 0 命中。AC-1（OOC 触发率 <1%）的评测集未建 | 管线未接入 |
| A5-02 | A5.1 危险场景例外机制 | 已实装（双信号必须同时满足：`world.is_dangerous=True` + `event_tags ⊇ {dangerous, danger, extreme, fatal, catastrophic}`），AC-2 已满足 | 已落地 |
| A5-03 | A5.1 输入预审 / 输出后审接入 chat 管线 | `scan_input` / `scan_output` 是独立 API，**没有**直接接到 `stubs/chat.py` 的 `complete()` / `stream_chunks()`（见 [notes.md:280-282](file:///Users/mofan/Documents/MyProjects/XiJian/docs/notes.md#L280-L282)） | 管线未接入 |
| A5-04 | A5.1 数据模型 `safety_audit_log` / `safety_rules` | 已实装为 `state.safety_audit_log` / `state.safety_rules` 进程内 dict（未落 SQLite）；旧版 `stubs/protection.py` 仍并存使用 `state.audits`，**新旧两套并行** | 部分落地 |
| A5-05 | A5.1 工具调用审计 | A5.2 mcp.check 已落 `mcp_audit` 表；A5.1 `safety_audit_log` 未扩 `stage='tool_call'` | 部分缺失 |
| A5-06 | A5.1 审查模块崩溃 → 降级最严格档 | 已实装（self-crash fallback 为 `hard_block`） | 已落地 |
| A5-07 | A5.2 电脑控制防护（MCP 防护） | server 侧已实装（见 §2.5）；**未接**：全局快捷键 ⌃⌥⌘Q / Win+Alt+Shift+Q、客户端 UI 弹窗、`kill -9` MCP 进程 | 部分缺失 |
| A5-08 | A5.3 自动快照部分缺失 | 已实装（见 §2.6）；**未接**：真实 zstd（用 zlib 模拟）、定时 tick 线程、A5.2 safety_stop confirm → A5.3 双写 | 部分缺失 |
| A5-09 | A5.4 过载 action handler 仍有 3 个无外部调用方 | `degrade_tts` / `compress_memory` / `emergency_dump` 仅 `register_action_handler` 被 `ACTION_SUSPEND_IDLE_NPCS` 调用（[npcs.py:962-971](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/npcs.py#L962-L971)），另三个无调用方，等 A6/A1.2/A1.1 起来时调用 | 集成未完成 |
| A5-10 | A5.4 档位阈值仅两档（严格/适中），不可关闭 | 已实装，与 v2.1 锁定值一致 | 已落地 |

### 3.7 A6 实时通话

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A6-01 | 全双工语音流（基于 STT/TTS） | 仅一次性 REST `/v1/audio/speech` 与 `/v1/audio/transcriptions`（[audio.py:14,47](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/audio.py)）；**无双向 WS 语音路由、无 barge-in、无流式 STT/TTS**（STT 后端为批处理） | 完全缺失 |
| A6-02 | `voice_calls` / `call_events` 表 | grep 全仓库（排除 macapp）无这两张表 | 数据模型缺失 |
| A6-03 | 模型动作联动（VRM 骨骼/BlendShape） | 运行时按情感/语义触发 motion 的代码缺失；motion 仅在 `devkit/motion_editor.py` 离线编辑器存在，**无运行时通话触发** | 完全缺失 |
| A6-04 | 通话特效/动画 | 角色配置触发的特效/简短动画无代码 | 完全缺失 |
| A6-05 | DiffSinger 歌声未接入通话 | `DiffSingerEngine`（`devkit/tts_engine.py:487`）仅为独立 dev TTS 引擎，**未接入任何通话/唱歌管线** | 完全缺失 |
| A6-06 | 端到端语音延迟 < 1.5s（本地） | 无实现，无法验证 | 完全缺失 |

### 3.8 A7 主动发起聊天或通话（仅 macOS/iOS）

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A7-01 | `character_initiated_actions` 表 | grep 无命中 | 数据模型缺失 |
| A7-02 | 主动决策逻辑（基于角色配置 [TODO: 主动频率上限] 与状态） | 仅 [ws_routes.py:207-217](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/ws_routes.py#L207-L217) 一个硬编码 demo 在连接 3s 后推固定消息 `char_yuki`，**无频率上限、无配置/状态驱动决策** | 仅占位实现 |
| A7-03 | 用户拒绝后「理解」写回记忆 | AC-2 未做 | 完全缺失 |
| A7-04 | 全局 / 按角色开关 | 非 macapp 代码中无开关；系统通知 / 来电接听 UI 属 macapp（排除） | 完全缺失 |

### 3.9 A8 桌宠 / 动态壁纸（仅 macOS）

| 编号 | 功能清单要求 | 代码现状 | 缺口分类 |
|------|--------------|----------|----------|
| A8-01 | `desktop_pets` / `dynamic_wallpapers` / `pet_action_log` 表 | grep 无命中 | 数据模型缺失 |
| A8-02 | 桌宠可审计日志 | `pet_action_log` 缺失；仅 [ws_routes.py:258](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/ws_routes.py#L258) 一个 no-op echo（`desktop_pet.command.echo`）+ `:256` 的 `emergency_pause` ack，**无持久化** | 完全缺失 |
| A8-03 | 壁纸模式写操作禁用强制 | AC-4 未做；权限矩阵无 macapp 外强制代码 | 完全缺失 |

### 3.10 B. Apple TouchBar & Dynamic Island

| 编号 | 问题 | 详述 |
|------|------|------|
| B-01 | 整章为 `[TODO: 本章待补]` | 非 macapp 代码无任何 `touchbar`/`dynamic_island` 实现；v2 spec 第 1295 行标注占位 |

### 3.11 C. Development Kit (开发者工具)

| 编号 | 问题 | 详述 | 证据 |
|------|------|------|------|
| C0-01 | DevKit 包位置与清单不符 | 清单要求位于 `core/xijian_api/devkit/`；实际为顶层 `devkit/` 包。独立性满足，但位置偏离（清单已在 v2.2 写明 `xijian_api/devkit/__init__.py`） | [devkit/](file:///Users/mofan/Documents/MyProjects/XiJian/devkit) |
| C1-01 | C1.1 事件类工厂 / 上限 / 冲突检测未实现 | DSL 编辑器与校验已做，但「一类事件批量实例化」「单世界上限 200 条」「触发条件互相冲突 → 拒绝保存」未实现（关联 A4-02） | [devkit/Problems.md:130](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/Problems.md) |
| C2-01 | C2.1 文本生成声音为桩 | `generate_voice_from_text`（[voice_cloner.py:201-205](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/voice_cloner.py#L201-L205)）直接 HTTP 501「语音合成（MeloTTS）功能仍在制作中」；`MeloTTSEngine` 存在却未接入此路径 | [voice_cloner.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/voice_cloner.py) |
| C2-02 | C2.1 声音克隆不克隆且跳过版权确认 | `clone_voice_from_file`（[voice_cloner.py:228-283](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/voice_cloner.py#L228-L283)）仅 `save_voice` 存参考文件，**无真实说话人适配**，且 AC-1（克隆前版权确认）未做 | 同上 |
| C2-03 | C2.1 TTSManager 引用未定义属性（疑似 Bug） | `get_engine`/`list_all_voices` 引用 `self._engines`，但仅定义 `self._tts_engines`/`self._singing_engines`（[tts_engine.py:694,697,704](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/tts_engine.py#L694-L704)），任意 `synthesize()` 抛 `AttributeError` | 同上 |
| C2-04 | C2.7 微调 / prompt 蒸馏为桩 | `start_finetuning_job`/`export_finetuning_dataset`（[dialog_editor.py:201-248](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/dialog_editor.py#L201-L248)）返回占位 dict，无真实训练/导出 | [dialog_editor.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/dialog_editor.py) |
| C2-05 | C2.8 AI 生成 VRM 非真实 | `generate_model_from_text`（[model_viewer.py:264-327](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/model_viewer.py#L264-L327)）要么下载整个任意 HF repo，要么写占位 `.glb` JSON（"Placeholder — full AI VRM generation requires MLX backend"）；无文本+参考图→VRM 1.0 生成或骨骼绑定 | [model_viewer.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/model_viewer.py) |
| C2-06 | C2.8 FBX 导入不支持 | `register_model` 直接拒绝 `.fbx`（`fbx_conversion_unavailable`，[model_viewer.py:55-64](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/model_viewer.py#L55-L64)）；转换仅能调外部 `UNITY_PATH`/`BLENDER_PATH` CLI，清单将 `.fbx` 列为支持导入格式 | 同上 |
| C2-07 | C2.8 VRM 1.0 校验仅部分 | `validate_model_format`（[model_viewer.py:140-185](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/model_viewer.py#L140-L185)）只查 glTF JSON 中 `VRM`/`VRMC_vrm` 扩展标记，**未校验完整 VRM 1.0 规范**；AC-3 模型大小上限（<50MB/<20MB）保存时不强制 | 同上 |
| C2-08 | C2.9 关键帧编辑 / 回放未实现 | 关键帧仅存 dict（[motion_editor.py:396-424](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/motion_editor.py#L396-L424)），无真实动画曲线编辑；回放未实现，正确性仅依赖骨骼名比对（`validate_motion_skeleton`）与外部 `bvh2vrm` CLI | [motion_editor.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/motion_editor.py) |
| C3-01 | C3 剧情无执行/模拟引擎 | `plot_editor` 仅持久化 `nodes.json`/`edges.json`（[plot_editor.py:96-149](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/plot_editor.py#L96-L149)），无「被模拟世界读取并执行」的 simulate/execute，AC-1/AC-2 绑定强制仅为数据层、未校验运行 | [plot_editor.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/plot_editor.py) |
| C4-01 | C4 AI 设计辅助整体不可用 | `auto_suggest`/`suggest_with_questions`（[ai_assistant.py:133,144-148,207-213](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/ai_assistant.py#L133-L213)）返回「暂不开放，请耐心等待」，真实 LLM 调用回退静态模板；US-C4-01/02、AC-2（AI 必须优先询问用户）未满足 | [ai_assistant.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/ai_assistant.py) |
| C4-02 | C4 `dev_ai_assist_log` 表未实现 | 用 JSON `assist_log.json` 近似，`source='ai_suggested'` 标记未跨 AI 产出字段强制；`ai_ratio` 为每事件代理值或直读调用方 payload，非清单的「字段计数」算法 | 同上 |
| C5-01 | C5 7z 调用形式与清单字面不符 | 清单写 `py7zr.SevenZipFile(mode='solid')`；实际 `pack_payload` 用 `SevenZipFile(target, mode="w", solid=True)`。功能等价固实，但非文档 API 形式 | 功能等价，可忽略 |
| C5-02 | C5 SMTP 默认值与清单严重不符 | 见 §5 文档冲突 6、7、8：默认 512MB（清单 1200MB）、默认 600s 冷却（清单 3600s）、默认 465 端口（清单 587）、默认 STARTTLS 关（清单 True） | [devkit/__init__.py:127-158](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L127-L158) |

### 3.12 API 文档与实际路由的不一致

| 编号 | 问题 | 详述 |
|------|------|------|
| API-01 | **api.md 严重滞后** | `api.md` 仅文档化到 v2.3 之前的路由（characters / interactions / worlds / memory / protection / sessions）。自 v2.4 起新增的以下路由在 `api.md` 中**完全未文档化**（仅在 `openapi.yaml` 中存在）：<br>- `/v1/xijian/events/*` (A4.1，34+ 路由)<br>- `/v1/xijian/scenes/*` (A4.3，POI/travel-modes/interactions 三套资源)<br>- `/v1/xijian/{economy,currencies,wallets}/*` (A4.4，30+ 路由)<br>- `/v1/xijian/safety/*` (A5.1，scan/rules/audit/policy)<br>- `/v1/xijian/mcp/*` (A5.2，check/rules/audit/policy/safety_stop/snapshots)<br>- `/v1/xijian/backups/*` (A5.3，snapshots/capacity/policy/prune)<br>- `/v1/xijian/overload/*` (A5.4，status/tier/metrics/events/recovery)<br>- `/v1/xijian/npcs/*` (A4.2 NPC CRUD + scheduling)<br>- `/v1/xijian/worlds/{id}/{audit,compute,environment,event-categories,reset,summary}` 等新增子资源 |
| API-02 | api.md 中 v1 接口与实际不符 | `GET /v1/characters/{cid}/memory/entries?type=&page=` 在 api.md 列出，实际路由 `GET /v1/xijian/memory/entries`（[xijian_memory.py:26](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_memory.py#L26)）；`PATCH/DELETE /v1/entries/{eid}` 实际为 `/v1/xijian/memory/entries/<id>` |
| API-03 | api.md 中保护模块接口未对齐 | api.md 列出 `/v1/xijian/protection/disable`（两步挑战）、`/rollback`、`/audit/export` 等接口，实际路由仅有 `/guard/preview`（[xijian_protection.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_protection.py)）；A5.1 安全模块走 `/v1/xijian/safety/*` 新命名空间，但 api.md 未更新 |
| API-04 | api.md `POST /v1/xijian/memory/search` 向量检索 | 实际 `routes/xijian_memory.py` 仅有关键词搜索，向量检索未实现（同 ARCH-03） |
| API-05 | api.md `POST /v1/xijian/memory/consolidate` / `forget` | 路由存在（[xijian_memory.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/xijian_memory.py)），但底层 stub 仅做基础操作 |
| API-06 | api.md `POST /v1/chat/abort` / `POST /v1/xijian/generation/abort` | openapi.yaml 已定义 `/chat/abort` 和 `/xijian/generation/abort`，实际路由已实装（[abort.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/abort.py)） | 已落地 |
| API-07 | WebSocket 事件类型表 | `ws_routes.py` 仅处理 `desktop_pet.command.echo`/`emergency_pause`、`client.cancel_request`、`ping`/`pong`。缺失 api.md §5.4 列出的 `character.proactive_message`/`world.event_occurred`/`memory.consolidated`/`protection.alert`/`generation.progress` 等核心事件——但 stub 已通过 `publish_event` 发出 `overload.triggered`/`event.fired`/`character.state.changed`（[overload.py:943](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/overload.py#L943)、[events.py:464](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/events.py#L464)、[character_state.py:728](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/character_state.py#L728)） | 部分缺失 |

---

## 4. 实现错误

| 编号 | 问题 | 详述 | 证据 |
|------|------|------|------|
| E-01 | `economy.py:_probability_hit` 用 `hash()` 而非 sha256 | [economy.py:208](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/economy.py#L208) 使用 `hash(("economy_crime", npc_id, world_id, bucket))`，受 `PYTHONHASHSEED` 影响——与 [events.py:_stable_hash_unit](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/events.py)（用 `hashlib.sha256`）风格不一致，跨进程可能产生不同判定结果 | 影响 AC-2「NPC 主动偷窃/诈骗必须有合理判定与冷却」的可重现性 |
| E-02 | `chat.py:stream_chunks` 跳过 recall 管线 | [chat.py:1203-1255](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/chat.py#L1203-L1255) 直接委托 `backend.chat()`，**不跑 `_run_recall_pipeline` / `loadContext` / `recall_memory`**；仅非流式 `complete()` 走强制召回。这意味着流式对话完全绕过 A1.2 记忆系统 | 影响 A1.2 AC-1「长期记忆在每次新对话 100% 必载入」 |
| E-03 | `characters.py` 仍保留 `live2d_model` 字段 | [characters.py:40,59,83](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/characters.py#L40) 三处仍保留 `live2d_model` 字段，违反 v2.1「彻底移除 Live2D」决策 | 残留字段违反决策 |
| E-04 | `protection.py` 与 `safety.py` 新旧两套并行 | 旧版 [protection.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/protection.py) 仍使用 `state.audits` + `guard_preview` 走 `/v1/xijian/protection/guard/preview` 路由；新版 [safety.py](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/safety.py) 使用 `state.safety_audit_log` + `scan_input`/`scan_output` 走 `/v1/xijian/safety/*` 路由。两者并存导致 audit log 分裂 | 影响审计可追溯性 |
| E-05 | `chat.py` MCP 工具调用未过 A5.1 scan_input | 即使 A5.2 gate 已接，但模型输入 prompt 仍**未过 A5.1 输入预审**——用户可通过 prompt injection 绕过工具调用，让模型在 `tool_calls` 阶段触发危险 action | 安全审计盲区 |
| E-06 | `interactions.py` 使用 `random.choice` | [interactions.py:115](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/stubs/interactions.py#L115) 用 `random.choice` 而非确定性哈希，与 events.py 概率触发器风格不一致，跨进程不可重现 | 测试可重现性问题 |
| E-07 | `devkit/__init__.py` SMTP 默认值与 docstring 自相矛盾 | [devkit/__init__.py:47-49](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L47-L49) docstring 写"≤ 1200 MB"，但 [line 148-153](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L148-L153) 实际默认 `512000000`（512 MB）。同一文件内 docstring 与代码默认值自相矛盾 | 同一文件内矛盾 |
| E-08 | `devkit/__init__.py` SMTP 冷却时间默认 600s（docstring 引用 spec AC-2 要求 3600s） | [devkit/__init__.py:154-158](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L154-L158) docstring 引用 spec AC-2 "1-hour rate-limit"，但默认 `600`（10 分钟）。频次限制被放宽 6 倍 | 与 spec AC-2 不符 |

---

## 5. 文档之间的冲突

### 5.1 关键（CRITICAL）级冲突

#### 冲突 1：默认监听地址 0.0.0.0 vs 127.0.0.1

| 文档 | 行号 | 原文 |
|------|------|------|
| [CoreStartupGuide.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L17) | 17 | `**默认监听地址**：\`0.0.0.0:18500\`（**无需任何环境变量即可启动**）` |
| [CoreStartupGuide.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L167) | 167 | `host = "0.0.0.0"      # 已默认开放外部访问` |
| [Dev.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L96) | 96 | `**Python API 程序** 监听 \`127.0.0.1\` 的随机空闲端口` |
| [Dev.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L569) | 569 | `**本地 API 只监听 127.0.0.1**：绝不允许监听 \`0.0.0.0\`` |
| [api.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/api.md#L25) | 25 | `Bearer Token，启动时随机生成，仅 \`127.0.0.1\` 访问` |
| [openapi.yaml](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L31-L32) | 31-32 | `description: 本地环回地址（仅监听 127.0.0.1，绝不暴露 0.0.0.0）` |

**冲突说明**：`CoreStartupGuide.md` 明确将默认监听地址设为 `0.0.0.0:18500`（开放外部访问），而 `Dev.md`、`api.md`、`openapi.yaml` 均明确要求"仅监听 127.0.0.1，绝不暴露 0.0.0.0"。这是**安全策略层面的直接矛盾**——前者允许局域网/外部访问，后者以安全隐私基线明确禁止。

**严重等级**：CRITICAL（安全策略冲突，可能造成未授权外部访问）

**建议**：统一为 `127.0.0.1`（按 Dev.md/api.md/openapi.yaml 安全基线），将 `0.0.0.0` 改为用户显式 opt-in 的环境变量配置。

#### 冲突 2：macOS 最低系统版本

| 文档 | 行号 | 原文 |
|------|------|------|
| [README.md](file:///Users/mofan/Documents/MyProjects/XiJian/README.md#L30) | 30 | `**操作系统与版本**：macOS >= 26.0` |
| [CoreStartupGuide.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L27) | 27 | `macOS \| 13.0 (Ventura)` |
| [Dev.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L19) | 19 | `推荐 macOS 26 Tahoe，最低 macOS 13 Ventura` |
| [BuildGuide.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/BuildGuide.md#L55) | 55 | `- **macOS**: Apple Silicon (arm64)，macOS 12+` |

**冲突说明**：四份文档对 macOS 最低版本给出四个不同数值：26.0 / 13.0 / 13（推荐 26）/ 12+。`chk20260715.md` 第 49 行已记录该冲突并标注"遵循README.md所述内容，CoreStartupGuide.md已经修改，等待确认"，但实际 [CoreStartupGuide.md:27](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L27) 仍写为 13.0，未与 README 对齐。

**严重等级**：CRITICAL（构建/打包基线无法统一判定，CI 与 PyInstaller 目标平台配置无所适从）

**建议**：以 README.md 的 `macOS >= 26.0` 为准（与 Apple 最新平台 API 对齐），将其他三份文档统一为 `26.0`。

### 5.2 高（HIGH）级冲突

#### 冲突 3：OpenAI 兼容远程 API / 云端推理战略定位

| 文档 | 行号 | 原文 |
|------|------|------|
| [Dev.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L26) | 26 | `**AI 完全本地运行**：不依赖云端 AI，所有推理在用户设备上完成。` |
| [Dev.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L672-L673) | 672-673 | `Q：能不能加上云端推理？ A：不在路线图内。本项目核心理念之一就是本地优先。` |
| [README.md](file:///Users/mofan/Documents/MyProjects/XiJian/README.md#L54-L55) | 54-55 | `有些用户的设备配置明显是不足以本地运行Ai的，因此，我们在未来版本也会允许用户连接到OpenAi兼容的Api服务，来正常体验Ai功能` |
| [AIBackend.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/AIBackend.md#L128) | 128 | `## 4. OpenAI 兼容远程后端`（章节标题，文档已落地该后端） |
| [BuildGuide.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/BuildGuide.md#L87) | 87 | `未安装的 AI 后端不会被打包，但 OpenAI 兼容远程后端始终可用。` |

**冲突说明**：Dev.md 把"完全本地 / 不上云"列为关键约束并把云端推理明确踢出路线图；但 README.md、AIBackend.md、BuildGuide.md、CoreStartupGuide.md 都把"OpenAI 兼容远程后端"作为已实现能力描述。

**严重等级**：HIGH（战略定位冲突，影响路线图叙述与外部承诺）

**建议**：Dev.md 是战略愿景文档，需要明确"本地优先 + 兼容 OAI 远程作为降级路径"的层次，避免与 README 的"未来版本允许连接 OpenAI 兼容 API"承诺冲突。

#### 冲突 4：DevKit SMTP 配置常量

| 文档 | 行号 | 原文 |
|------|------|------|
| [Dev. Function List功能清单v2.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.%20Function%20List功能清单v2.md#L1756-L1764) | 1756-1764 | `SMTP_PORT=587` / `SMTP_USE_TLS=True` / `USER=xijian-dev@example.com` / `PASSWORD=REPLACE_BEFORE_DEPLOY` / `RECIPIENT=xijian-submissions@example.com` / `FROM=xijian-dev@example.com` |
| [devkit/__init__.py](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L127-L158) | 127-158 | `PORT=465`（默认）/ `USE_TLS=False`（默认）/ `USER=""`（默认空串）/ `PASSWORD=""`（默认空串）/ `RECIPIENT=config.DEFAULT_RECIPIENT` / `FROM=submissions@example.com` |

**冲突说明**：spec 与实现存在多处不一致：
- SMTP_PORT：spec 587（STARTTLS 端口）vs 实现 465（SSL 端口）
- SMTP_USE_TLS：spec `True`（默认启用 STARTTLS）vs 实现 `False`（默认关）
- HOST/USER/PASSWORD：spec 给出示例值，实现默认空串
- FROM_ADDR：spec `xijian-dev@example.com` vs 实现 `submissions@example.com`
- 环境变量名前缀：spec 用 `DEV_SUBMIT_*`，实现用 `XIJIAN_DEV_*`

**严重等级**：HIGH（C5 提交链路功能性与 Apifox 测试基线不一致）

#### 冲突 5：DevKit 附件大小上限

| 文档 | 行号 | 原文 |
|------|------|------|
| [Dev. Function List功能清单v2.md](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.%20Function%20List功能清单v2.md#L1763) | 1763 | `\`DEV_SUBMIT_MAX_ATTACHMENT_BYTES\` \| 单次提交上限 \| \`1_200_000_000\`` |
| [devkit/__init__.py:47-49 docstring](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L47-L49) | 47-49 | `The archive (after packing) must be **≤ 1200 MB**` |
| [devkit/__init__.py:148-153 代码](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L148-L153) | 148-153 | `DEV_SUBMIT_MAX_ATTACHMENT_BYTES: int = int(os.environ.get("XIJIAN_DEV_MAX_BYTES", "512000000") or "512000000")` |

**冲突说明**：spec 与 devkit docstring 都要求 1_200_000_000 字节（1200 MB），但 devkit 实际默认值是 512000000 字节（512 MB）。**同一文件内 docstring 与代码默认值自相矛盾**，且与 spec 不符。

**严重等级**：HIGH（用户提交会被意外 413 拒绝）

#### 冲突 6：openapi.yaml 内部 MemoryEntry schema 冲突

| 文档 | 行号 | 原文 |
|------|------|------|
| [openapi.yaml:9441-9458](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L9441-L9458) | MemoryEntryCreate schema | `attributes: type: object` / `importance: type: string, enum: [low, medium, high]` / `decay: type: string, enum: [never, slow, normal, fast]` |
| [openapi.yaml:9477-9480](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L9477-L9480) | MemoryEntry schema | `importance: type: number, minimum: 0, maximum: 1`（无 enum） |
| [openapi.yaml:4803-4808](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L4803-L4808) | `/xijian/memory/entries` POST 响应 inline | `attributes: type: string` / `importance: type: string` |

**冲突说明**：openapi.yaml 内部对 MemoryEntry 的字段定义存在三套互斥 schema：1) `MemoryEntryCreate` 把 `attributes` 声明为 object；2) `MemoryEntry` 把 `importance` 声明为 number；3) 端点 inline 响应把 `attributes` 声明为 string。客户端无法生成一致的数据模型。

**严重等级**：HIGH（OpenAPI 契约内部自相矛盾，代码生成器无法稳定产出客户端）

### 5.3 中（MEDIUM）级冲突

#### 冲突 7：iOS / Android 平台支持状态

| 文档 | 行号 | 原文 |
|------|------|------|
| [README.md:14](file:///Users/mofan/Documents/MyProjects/XiJian/README.md#L14) | 14 | `iOS适配有计划，但不是现在，我们现在需要专注于项目的核心内容` |
| [README.md:30](file:///Users/mofan/Documents/MyProjects/XiJian/README.md#L30) | 30 | `**操作系统与版本**：iOS >= 26.0, Android >= 12.0` |
| [CoreStartupGuide.md:29-30](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L29-L30) | 29-30 | `\| iOS \| iOS >= 26.0 \|` / `\| Android \| Android >= 12.0 \|` |
| [Dev.md:20](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L20) | 20 | `\| iOS / iPad \| 暂不支持 \| — \| — \| 目前项目无 iOS 上架计划 \|` |
| [Dev.md:643](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L643) | 643 | `M6 — iOS / Android（待评估）` |

**严重等级**：MEDIUM（用户预期与开发基线不一致）

#### 冲突 8：Mac 最低内存与磁盘配置

| 文档 | 行号 | 原文 |
|------|------|------|
| [README.md:34](file:///Users/mofan/Documents/MyProjects/XiJian/README.md#L34) | 34 | `对于Mac设备，M系列芯片是必须的，16GB内存为最低，其中可用内存不低于6GB，硬盘可用空间不低于24GB` |
| [Dev.md:19](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L19) | 19 | `推荐内存 64–128 GB；可用磁盘 ≥ 32 GB` |
| [CoreStartupGuide.md:27](file:///Users/mofan/Documents/MyProjects/XiJian/docs/CoreStartupGuide.md#L27) | 27 | `macOS \| 13.0 (Ventura) \| **等待填写**`（推荐配置未填写） |

**严重等级**：MEDIUM（影响用户购机/部署决策）

#### 冲突 9：DevKit 提交冷却时间

| 文档 | 行号 | 原文 |
|------|------|------|
| [Dev. Function List功能清单v2.md:1764](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.%20Function%20List功能清单v2.md#L1764) | 1764 | `\`DEV_SUBMIT_COOLDOWN_SECONDS\` \| 频次冷却时间 \| \`3600\`` |
| [devkit/__init__.py:40-42 docstring](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L40-L42) | 40-42 | `Each \`developer_id\` may submit at most **once per hour**` |
| [devkit/__init__.py:154-158 代码](file:///Users/mofan/Documents/MyProjects/XiJian/devkit/__init__.py#L154-L158) | 154-158 | `DEV_SUBMIT_COOLDOWN_SECONDS: int = int(os.environ.get("XIJIAN_DEV_COOLDOWN_SECONDS", "600") or "600")` |

**严重等级**：MEDIUM（频次限制被放宽 6 倍）

#### 冲突 10：视频生成端点响应状态码

| 文档 | 行号 | 原文 |
|------|------|------|
| [openapi.yaml:1771-1782](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L1771-L1782) | 1771-1782 | `/videos/generations` POST 的 responses：`'200' 成功` / `'201' 创建成功` / `'400' BadRequest` |
| [videos.py:54-56](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/videos.py#L54-L56) | 54-56 | `response.status_code = 202` |
| [api.md:68-69](file:///Users/mofan/Documents/MyProjects/XiJian/docs/api.md#L68-L69) | 68-69 | `204 \| 成功无 body`（通用规范中 202 未列入） |

**严重等级**：MEDIUM（Apifox/客户端契约失配）

#### 冲突 11：视频 remix 端点参数类型

| 文档 | 行号 | 原文 |
|------|------|------|
| [openapi.yaml:1837-1840](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L1837-L1840) | 1837-1840 | `seconds: type: string` / `fps: type: string`（在 `/videos/{video_id}/remix`） |
| [openapi.yaml:1765-1768](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L1765-L1768) | 1765-1768 | `seconds: type: integer` / `fps: type: integer`（在 `/videos/generations`） |
| [videos.py:91-92](file:///Users/mofan/Documents/MyProjects/XiJian/core/xijian_api/routes/videos.py#L91-L92) | 91-92 | `"seconds": int(payload.get("seconds", ...))`（两处都用 int 强转） |

**严重等级**：MEDIUM（同一参数跨端点类型不一致）

#### 冲突 12：Memory entry attributes 结构跨文档一致性

| 文档 | 行号 | 原文 |
|------|------|------|
| [api.md:650-657](file:///Users/mofan/Documents/MyProjects/XiJian/docs/api.md#L650-L657) | 650-657 | `"attributes": { "importance": "high", "decay": "never", "category": "preference" }`（嵌套对象） |
| [Dev.md:658-662](file:///Users/mofan/Documents/MyProjects/XiJian/docs/Dev.md#L658-L662) | 658-662 | `"attributes": { "importance": "high", "decay": "never", "category": "preference" }`（嵌套对象） |
| [openapi.yaml:9441-9458](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L9441-L9458) | 9441-9458 | `attributes` 为 object，子字段 importance/decay/category（与 api.md/Dev.md 一致） |
| [openapi.yaml:4803-4808](file:///Users/mofan/Documents/MyProjects/XiJian/docs/openapi.yaml#L4803-L4808) | 4803-4808 | `attributes` 为 string，且 importance/decay/category 被扁平化到响应顶层（与 api.md/Dev.md 不一致） |

**严重等级**：MEDIUM（端点响应结构 vs 规格定义不一致）

---

## 6. 测试结果

### 6.1 测试环境

- conda 环境：`xijianBase`（位于 `/opt/anaconda3/envs/xijianBase`）
- 工作目录：`/Users/mofan/Documents/MyProjects/XiJian/core`
- 命令：`python -m pytest --tb=no -q`

### 6.2 测试结果

```
1667 passed in 43.37s
```

- 总数：**1667 个测试全部通过**
- 失败：0
- 跳过：0
- 回归：0

### 6.3 测试覆盖分布（按文件）

| 测试文件 | 覆盖章节 |
|----------|----------|
| test_auth.py / test_healthz.py / test_root_version.py | 鉴权与健康检查 |
| test_chat_sync.py / test_chat_stream_sse.py / test_chat_stream_ndjson.py / test_chat_abort.py | A2 聊天管线 |
| test_models.py / test_openai_backend.py / test_mock_backend.py | AI 后端抽象层 |
| test_files.py | A2 文件接口 |
| test_idempotency.py / test_errors_dual_format.py | 通用规范 |
| test_mcp_server.py / test_xijian_mcp.py / test_xijian_mcp_rules.py | A5.2 MCP 防护 |
| test_xijian_backups.py | A5.3 自动备份 |
| test_xijian_character_state.py / test_xijian_characters.py | A3 角色与状态 |
| test_xijian_citations.py | A1.2 引用审查 |
| test_xijian_economy.py / test_xijian_world_currencies.py / test_xijian_wallets.py / test_xijian_transactions.py | A4.4 经济系统 |
| test_xijian_events.py | A4.1 事件调度 |
| test_xijian_load_context.py / test_xijian_memory.py / test_xijian_memory_decay.py | A1 记忆系统 |
| test_xijian_mcp_rules.py | A5.2 规则库 |
| test_xijian_npcs.py | A4.2 NPC 调度 |
| test_xijian_overload.py | A5.4 过载防护 |
| test_xijian_pois.py / test_xijian_travel_modes.py / test_xijian_scene_interactions.py | A4.3 场景与互动 |
| test_xijian_protection.py / test_xijian_safety.py / test_xijian_safety_rules.py | A5.1 安全审查 |
| test_xijian_recall_pipeline.py | A1.2 强制召回 |
| test_xijian_safety_rules.py | A5.1 规则库 |
| test_xijian_transactions.py | A4.4 交易日志 |
| test_xijian_travel_modes.py | A4.3 旅行模式 |
| test_xijian_wallets.py | A4.4 钱包 |
| test_xijian_world_currencies.py / test_xijian_worlds.py | A4.2 世界系统 |
| test_ws.py | WebSocket |

### 6.4 测试覆盖缺口

| 编号 | 缺口 | 详述 |
|------|------|------|
| TEST-01 | 核心功能无集成测试 | `core/tests/` 仅有单元测试（stub 级），缺乏跨模块集成测试（如 A1.2 记忆→A2 聊天管线、A4.1 事件→A1 记忆回写、A5.4 过载→A4.2 NPC 调度） |
| TEST-02 | 保护模块覆盖率不足 | `Dev.md:611` 要求保护模块 90% 行覆盖率，实际 `stubs/protection.py` 核心分支（OOC/例外/崩溃降级）无测试覆盖 |
| TEST-03 | AI 抽象层无 mock backend 测试 | `Dev.md:612` 要求 AI 抽象层有 mock backend 测试，实际 `ai/backends/mock.py` 仅为占位，无契约测试 |
| TEST-04 | 跨平台握手流程无集成测试 | `Dev.md:613` 要求有集成测试跑完整握手，实际无 |
| TEST-05 | 无端到端性能基线测试 | `Dev.md §10` 定义了 TTFT/内存增长/渲染帧率基线，但 CI 中无性能回归测试 |
| TEST-06 | 无安全审计测试 | A5.1/A5.2 关键安全模块无注入攻击/OOC 绕过/越权操作的红队测试用例 |
| TEST-07 | A5.1/A5.2 未接 chat 管线的端到端测试 | 由于 chat.py 未调用 `safety.scan_input` / `scan_output`，无法验证"输出真的被审查" |
| TEST-08 | 流式路径无 recall 管线测试 | 由于 `stream_chunks()` 跳过 `_run_recall_pipeline()`，无法验证流式对话的记忆召回 |

---

## 7. 与上一份报告（chk20260715）的对比

### 7.1 已解决项目（自 2026-07-15）

| chk20260715 编号 | 状态 | 备注 |
|------------------|------|------|
| A4-08 (POI/交通/互动) | 已解决 | v2.5 A4.3 实装 |
| A4-09 (经济系统) | 已解决 | v2.6 A4.4 实装 |
| A5-01 (OOC 检测) | 部分解决 | v2.7 实装 stub，但**未接 chat 管线** |
| A5-02 (危险场景例外) | 已解决 | v2.7 实装 |
| A5-04 (数据模型 safety_audit_log/safety_rules) | 已解决 | v2.7 实装 |
| A5-06 (审查模块崩溃降级) | 已解决 | v2.7 实装 self-crash fallback |
| A5-07 (MCP 防护) | 部分解决 | v2.8 实装 server 侧，**未接客户端快捷键/UI** |
| A5-08 (自动快照) | 部分解决 | v2.9 实装，**用 zlib 替代 zstd、未起定时 tick** |

### 7.2 未解决项目（自 2026-07-15 仍未修复）

| chk20260715 编号 | 当前状态 |
|------------------|----------|
| ARCH-01 (无 SQLite 持久化) | **未解决**，仍是进程内 dict |
| ARCH-02 (无 MCP 服务端) | **已部分解决**——`xijian_api/mcp/` 子包已实装，server 侧有 |
| ARCH-03 (AI 后端引擎与 v2.1 选型不符) | **未解决** |
| ARCH-04 (bge-m3 未接入) | **未解决** |
| ARCH-05 (核心服务层缺失) | **未解决** |
| A1-01 ~ A1-10 (记忆系统大部分项) | **未解决**（仅 A1-03 `deleted_at` 字段定义存在但仍硬删除） |
| A2-01 ~ A2-05 (AI 模块) | **未解决** |
| A3-01 ~ A3-04 (角色与状态) | **未解决**，`live2d_model` 残留 |
| A4-01 ~ A4-07 (模拟世界核心) | **未解决**（场景生成、内置事件库、NPC 自动生成/自我决策、记忆回写均无） |
| A5-03 (输入预审 / 输出后审接入 chat) | **未解决** |
| A5-05 (工具调用审计) | **部分解决**（A5.2 mcp_audit 已接，但 A5.1 safety_audit_log 未扩 tool_call stage） |
| A5-09 (过载 action handler 集成) | **未解决** |
| A6-01 ~ A6-05 (实时通话全部) | **未解决** |
| A7-01 ~ A7-04 (主动发起全部) | **未解决** |
| A8-01 ~ A8-03 (桌宠/壁纸全部) | **未解决** |
| B-01 (Apple TouchBar/Dynamic Island) | **未解决** |
| C2-01 ~ C2-08 (DevKit 多项) | **未解决** |
| C4-01 ~ C4-02 (AI 设计辅助) | **未解决** |
| DOC-01 ~ DOC-13 (文档冲突 13 项) | **未解决 7 项**（DOC-01/02/03/04/05/06/07 仍存在；DOC-08/11/12 已因 CoreWinSupport.md 弃置而消失；DOC-09/10 部分因 v2.6 实装经济但仍存在 SMTP 默认值不符） |
| API-01 ~ API-13 (API 文档不符) | **未解决**，且 api.md 严重滞后于 openapi.yaml |
| CFG-01 ~ CFG-06 (配置部署不符) | **未解决 5 项** |

### 7.3 新发现问题（自 2026-07-15 之后引入）

| 编号 | 问题 | 来源 |
|------|------|------|
| NEW-01 | A5.1 / A5.2 stub 已落地但**未接 chat 管线** | v2.7 / v2.8 章节实装但 pipeline 集成留作后续 |
| NEW-02 | A5.3 用 `zlib` 替代 `zstd` | v2.9 notes.md 已记录 |
| NEW-03 | A5.3 未起定时 tick 线程 | v2.9 notes.md 已记录 |
| NEW-04 | A5.2 safety_stop confirm → A5.3 双写未接 | v2.9 notes.md 已记录 |
| NEW-05 | `economy.py:_probability_hit` 用 `hash()` 受 `PYTHONHASHSEED` 影响 | v2.6 实装时与 events.py 风格不一致 |
| NEW-06 | `protection.py` 与 `safety.py` 新旧两套并行 | v2.7 起并存 |
| NEW-07 | `devkit/__init__.py` SMTP 默认值与 docstring 自相矛盾 | v2.3 实装遗留 |
| NEW-08 | `api.md` 严重滞后于 `openapi.yaml` | v2.4~v2.9 新增 100+ 路由未文档化到 api.md |
| NEW-09 | `openapi.yaml` 内部 MemoryEntry schema 三套互斥 | 历史遗留 |
| NEW-10 | `openapi.yaml` 视频 remix 参数类型与 generations 不一致 | 历史遗留 |

---

## 8. 附录：检查方法

### 8.1 检查范围

1. **文档扫描**：读取 `docs/` 下所有 `.md` 文件（排除 `已弃置/`、`md2pdf/`），共 11 份现行文档 + `openapi.yaml`。
2. **代码扫描**：遍历 `core/xijian_api/`、`devkit/` 目录下所有 `.py` 文件（排除 `__pycache__`、`.venv`、测试文件），重点对比 `stubs/` 实现与 `routes/` 暴露接口。
3. **基准文档**：以 `docs/Dev. Function List功能清单v2.md` (v2.9, 2026-07-22) 为唯一功能权威来源，逐节核对 AC-*、US-*、接口定义、数据模型表、Mermaid 流程。
4. **已知问题库**：参考 `docs/chk20260715.md`（2026-07-15 盘账）作为已确认缺口基线，在此基础上补全新发现的实现错误、文档不一致、API 契约不符。
5. **测试执行**：在 conda 环境 `xijianBase` 中执行 `python -m pytest --tb=no -q`，验证 1667 个测试全部通过。
6. **排除范围**：`macapp/` 目录（macOS 原生 App 代码）、`docs/已弃置/` 目录、第三方依赖代码（`core/.venv/`、`devkit/.venv/`）。

### 8.2 检查工具

- `Read`：直接读取文件源码验证
- `Grep`：跨文件搜索关键词、API 路径、字段名
- `LS`：目录结构遍历
- `SearchCodebase`：语义搜索代码意图
- 子代理任务并行：stubs 审计、文档冲突审计

### 8.3 关键发现汇总

| 类别 | 数量 | 备注 |
|------|------|------|
| 仍未实现的功能项 | 56 | 集中在 A1/A2/A3/A6/A7/A8/C 章节 |
| 实现错误 | 8 | 含 1 个跨进程不一致（hash 函数）+ 1 个流式路径跳过 recall |
| 文档间冲突 | 12 | CRITICAL 2 / HIGH 4 / MEDIUM 6 |
| 测试覆盖缺口 | 8 | 集成测试 / 性能 / 安全审计 / 流式路径 |
| **合计待处理项** | **84** | 较 chk20260715 的 107 项减少 23 项（因 6 个章节实装） |

### 8.4 优先建议

按"阻断性 / 影响面 / 修复成本"三维度排序的优先修复建议：

| 优先级 | 编号 | 修复项 | 理由 |
|--------|------|--------|------|
| P0 | ARCH-01 | SQLite 持久化层 | 所有 stub 重启即丢，阻断生产可用性 |
| P0 | 冲突 1 | 监听地址统一为 127.0.0.1 | 安全策略冲突，可能造成未授权外部访问 |
| P0 | 冲突 2 | macOS 最低版本统一 | 构建基线漂移 |
| P1 | A5-03 | A5.1 接入 chat 管线 | 已实装 stub 但未生效，等于没做 |
| P1 | A1-08 | 流式路径接入 recall 管线 | 流式对话绕过核心记忆 |
| P1 | E-04 | `protection.py` 与 `safety.py` 合并 | 新旧两套并行导致 audit log 分裂 |
| P1 | API-01 | `api.md` 补齐新增 100+ 路由文档 | Apifox 测试基线严重滞后 |
| P1 | 冲突 4/5/9 | DevKit SMTP/体积/冷却默认值与 spec 对齐 | C5 提交链路功能性 |
| P2 | A2-01 | 角色上下文注入顺序（人设/状态/世界） | 上下文组装缺失 |
| P2 | A2-03 | 多模态输入降级占位 | 多模态输入丢失 |
| P2 | A3-02 | 移除 `live2d_model` 残留字段 | 违反 v2.1 决策 |
| P2 | A4-06 | NPC 自我决策接入 LLM | 核心决策环节空转 |
| P2 | ARCH-03/04 | AI 后端选型落地 + bge-m3 接入 | 影响 A1.2 向量检索、A2 多模态 |
| P3 | A6/A7/A8 | 实时通话 / 主动发起 / 桌宠 全部 | 完全缺失，需独立设计章节 |
| P3 | B-01 | Apple TouchBar / Dynamic Island | spec 标注占位 |

---

> 本报告由开发者工具审计生成，所有引用的文件路径、行号与原文均经过实际读取验证。如需复现检查过程，可按 §8 附录的方法执行。
