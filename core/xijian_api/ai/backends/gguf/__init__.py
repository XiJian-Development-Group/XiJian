"""GGUF 后端包（llama.cpp 本地推理）。

该子包通过 llama.cpp 的 ``llama-cpp-python`` 绑定提供本地推理实现。
适用于 CPU 和 GPU（Apple Silicon 上的 Metal 加速）。

GGUF backends package (llama.cpp local inference).

This sub-package provides local inference implementations via
llama.cpp's ``llama-cpp-python`` bindings.  Works on CPU and GPU
(Metal-accelerated on Apple Silicon).
"""
