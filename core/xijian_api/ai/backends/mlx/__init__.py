"""MLX 后端包（Apple Silicon 本地推理）。

该子包提供基于 Apple MLX 框架的本地推理实现。仅在 Apple Silicon (M系列)
芯片上可用。

MLX backends package (Apple Silicon local inference).

This sub-package provides local inference implementations powered by
Apple's MLX framework.  Available on Apple Silicon (M-series) chips only.
"""
