"""模拟后端包（测试和本地开发用）。

该子包提供模拟后端实现，用于测试和本地开发。
它们从不加载真实权重，始终 ``is_available()``。

Mock backends package (for tests and local development).

This sub-package provides mock backend implementations for tests and
local development.  They never load real weights and are always
``is_available()``.
"""
